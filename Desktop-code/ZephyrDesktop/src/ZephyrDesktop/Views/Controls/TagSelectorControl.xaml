<UserControl x:Class="ZephyrDesktop.Views.Controls.TagSelectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <UserControl.Resources>
        <Style x:Key="DarkCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#e2e8f0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="CheckBorder"
                                    Width="16" Height="16" CornerRadius="3"
                                    BorderBrush="#475569" BorderThickness="1"
                                    Background="Transparent"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center">
                                <Path x:Name="CheckMark"
                                      Data="M3,8 L6.5,11.5 L13,4"
                                      Stroke="White" StrokeThickness="2"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background" Value="#3B82F6"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#3B82F6"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border x:Name="TriggerBorder"
                CornerRadius="6" MinHeight="36"
                Padding="8,6" BorderBrush="#334155" BorderThickness="1"
                Cursor="Hand"
                MouseLeftButtonDown="TriggerBorder_MouseLeftButtonDown">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#1e293b"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#253347"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <WrapPanel Grid.Column="0" x:Name="SelectedTagsPanel" Orientation="Horizontal"/>
                <TextBlock Grid.Column="0" x:Name="PlaceholderText"
                           Text="+ 添加标签" FontSize="13" Foreground="#94a3b8"
                           VerticalAlignment="Center"/>
                <Path Grid.Column="1" Data="M0,0 L4,4 L8,0"
                      Stroke="#64748b" StrokeThickness="1.5"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      HorizontalAlignment="Right" VerticalAlignment="Center"
                      Margin="8,0,2,0"/>
            </Grid>
        </Border>
        <Popup x:Name="DropDownPopup"
               PlacementTarget="{Binding ElementName=TriggerBorder}"
               Placement="Bottom" StaysOpen="True"
               PopupAnimation="Slide" AllowsTransparency="True"
               Closed="DropDownPopup_Closed">
            <Border Background="#1e293b" CornerRadius="6" Padding="4" Margin="0,2,0,0"
                    BorderBrush="#334155" BorderThickness="1"
                    MinWidth="{Binding ActualWidth, ElementName=TriggerBorder}">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="240">
                        <ItemsControl x:Name="TagsList" Margin="2">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource DarkCheckBoxStyle}"
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              Checked="TagCheckBox_Changed"
                                              Unchecked="TagCheckBox_Changed"
                                              Margin="4,3">
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="10" Height="10" Margin="0,0,8,0"
                                                     Fill="{Binding ColorBrush}"
                                                     VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Name}" FontSize="13"
                                                       Foreground="#e2e8f0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </CheckBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <TextBlock x:Name="EmptyText" Text="暂无标签" FontSize="13"
                               Foreground="#64748b" HorizontalAlignment="Center"
                               Margin="0,12" Visibility="Collapsed"/>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>
