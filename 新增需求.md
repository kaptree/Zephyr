# 新增需求

1、✅ 已完成：目前新建的便签任务，在进行编辑和创建的时候，不能进行回车和换行，并且没有富文本编辑功能。现在需要对便签编辑的任务进行优化，添加回车和换行功能，以及富文本编辑功能，并且编辑和创建便签的时候弹窗页面剧中进行，页面布局也更加美观和宽阔。

2、✅ 已完成：目前指派他人任务的时候不能对多人勾选，只能勾选单人，现在需要对指派他人任务的功能进行优化，添加对多人勾选的功能，并且针对一个小组的所有人能一键全部勾选。

3、✅ 已完成：指派他人任务的时候，没有反馈机制，指派的任务被完成后，被指派的人应该能对当前完成的工作进行反馈填报，并且通过系统通知的方式，通知指派任务的人。其中可能涉及到需要新建一个消息队列的机制，在系统的右上角显示通知的消息，通知的人可以点击消息，查看通知的内容。也可以点击每一个消息进去查看详情，这个可能要设计几张存储的表结构，并且补全用户之间聊天的功能，和消息推送系统提示音等一些列内容，请针对上述内容对项目进行设计方案并进行调整和测试。

- 实现内容：
  - 新增 `notifications`（通知）与 `chat_messages`（聊天）两张表结构，AutoMigrate 自动建表
  - 通知事件：任务指派 `task_assigned`、任务完成 `task_completed`、任务反馈 `task_feedback`、催办提醒 `task_remind`
  - 完成/反馈任务时被指派者填报反馈内容（富文本），系统自动通知任务发起人
  - 右上角通知铃铛：未读数角标、下拉通知列表、点击消息查看详情、可跳转对应任务、一键全部已读、单条删除
  - 用户间一对一聊天：会话列表（未读数）、消息记录、实时 WebSocket 推送、任务详情「联系」入口直达会话
  - 实时消息通道：`GET /ws/user/:user_id`（个人通知通道），新通知/新消息浏览器提示音（Web Audio 合成，无需外部资源）
  - 通知/聊天/反馈 API：`/notifications`、`/chat`、`/reminders`、`POST /notes/:id/feedback`
  - 修复：被指派者（assignee）可完成任务并提交反馈（原权限仅允许发起人完成）

4、✅ 已完成：通知中心这里有一个按钮可以跳转一个新页面可以查看所有通知内容，通知太多了就显示最近十条通知，其他通知就是到面板查看，并且当前的任务完成后的反馈内容需要添加到任务工作台的指派便签中，同时同步指派人和被指派人的便签内容和状态，通知中心的查看任务，无法将任务便签弹出来，请修复和完成上述内容，并做好测试工作

- 实现内容：
  - 新增「通知中心」独立页面 `/notifications`：展示全部通知（分页/加载更多、只看未读筛选、全部已读、单条删除、查看任务、返回工作台）
  - 铃铛下拉面板只显示最近 10 条通知，底部提供「查看全部通知 →」按钮跳转通知中心
  - 任务反馈同步到便签：任务列表/详情接口预加载 assignees（含 feedback_content），工作台「已完成」便签卡片显示绿色「任务反馈」区块（被指派人姓名 + 反馈内容），指派人与被指派人看到同一任务便签内容和状态
  - 修复「查看任务」无法弹出任务便签：改为监听 `route.query.note` 变化（同页面再次点击也能触发），归档任务通过详情接口归一化后打开详情面板

5、✅ 已完成：在系统管理的AI服务配置板块，需要新增一个支持dify的API调用的添加选项，并且针对添加的模型能够一键测试是否联通，连通后才能能保存并使用。

- 实现内容：
  - AI 服务商新增 `dify` 类型（openai/deepseek/qwen/zhipu/dify/custom），新增服务商下拉选择，自动填充名称与地址占位提示
  - 新增「一键测试连通」按钮与 `POST /api/v1/system/ai-configs/test` 接口（不落库）：dify 走 `GET {endpoint}/info` 探测（兼容带/不带 `/v1`，返回应用名），其他走 OpenAI 兼容 `POST /chat/completions`（max_tokens=1）
  - 测试结果展示：连通成功（绿色徽标 + 延迟 ms）/ 失败（红色徽标 + 错误信息）
  - 保存门禁：创建/更新 AI 配置前后端强制连通性测试，测试失败返回 400 拒绝保存；前端保存按钮在未通过测试时禁用
  - dify 业务调用：`POST /chat-messages`（blocking 模式），供 AI 报表/日报等功能复用；统一 `services.CallAIService` 双协议（OpenAI 兼容 / Dify）
  - AI 配置新增 `provider_type` 字段（旧数据归一化为 openai）

6、✅ 已完成：指派任务和完成指派任务后页面需要局部刷新一下，将最新的任务面板自动刷新出来，并且在指派任务的时候会有一个工作时间选项，任务下发后盯办任务的左上角就开始倒计时，任务时间不足的时候会通过通知的方式发送提醒。

- 实现内容：
  - 指派/协作任务表单新增「⏱ 工作时间」选项（不限制/1小时/2小时/4小时/8小时/1天/2天/3天/7天），后端按工作时间自动计算截止时间 `due_time`
  - 任务便签左上角实时倒计时：显示「剩余 X天X小时 / X小时X分钟 / 已超时 X」，每 30 秒自动刷新；剩余不足 1 小时或已超时显示红色警示，未完成的任务才显示（完成/归档后隐藏）
  - 到期提醒通知：新增后端调度器（`auto_remind_enabled` 开启，按 `auto_remind_interval_minutes` 间隔扫描），距截止不足 2 小时且未提醒过的未完成任务，向负责人/发起人/被指派人发送「任务到期提醒」通知（`task_due` 类型，WebSocket 实时推送，`due_remind_at` 字段去重）
  - 局部刷新：指派任务创建成功、任务完成归档（反馈提交）后自动刷新任务面板，最新任务/状态/倒计时立即展示

7、✅ 已完成：工作成效板块需要进行调整，添加一个团队报告的模块，通过团队报告模块，可以查看团队成员的工作成效，包括任务完成率、任务完成时间、任务完成数量等，并且可以按照选择时间范围生成周报。修改周报生成逻辑，生产按钮旁边新增一个模型选择，若无模型则是模板生产，若选择了模型，则使用选择的模型生成周报。

- 实现内容：
  - 工作成效页新增「👥 团队报告」Tab：时间范围选择（本周/本月/自定义日期），成员成效统计表（成员/部门/创建任务/完成任务/完成率/平均完成耗时/被盯办），顶部汇总卡片（成员数/创建数/完成数/整体完成率）
  - 权限范围：super_admin 查看全部部门成员，其他角色查看本部门（含子部门）成员
  - 团队统计接口 `GET /analytics/team-stats`：按时间范围聚合各成员任务完成率、平均完成耗时、完成数量、被盯办次数
  - 团队周报生成 `POST /analytics/team-report`：生成按钮旁新增 **「🤖 生成模型」选择下拉**（读取启用的 AI 配置，普通用户可访问 `GET /analytics/ai-configs` 简表接口）；未选择模型 → 模板生成（成员成效表格+总结）；选择模型 → 调用所选 AI 模型智能生成（失败自动回退模板）
  - 生成的团队报告自动保存至报告历史，支持复制/下载

8、✅ 已完成：团队周报这里每一个人可以勾选几个人设置为团队，不需要将每个人都拉进来，然后再针对勾选组建的团队可以生成报告，生成的报告属于当前用户。

- 实现内容：
  - 团队报告成员明细表新增勾选复选框：可自由连续勾选任意成员组建自定义团队（表格始终显示全部成员供勾选，勾选行蓝色高亮），并提供「全选 / 清空（全部成员）」快捷操作
  - 勾选后汇总卡片（团队成员数/创建任务/完成任务/整体完成率）实时按勾选成员本地过滤计算，提示「✅ 已勾选 X 人组建团队」
  - 生成周报/月报时携带 `user_ids`（`GET /analytics/team-stats` 的 `user_ids` 查询参数、`POST /analytics/team-report` 的 `user_ids` 字段），后端仅在当前用户可见成员范围内过滤，统计与报告只包含勾选成员
  - 未勾选任何成员时保持统计全部可见成员（原行为不变）
  - 生成的报告归属当前用户（WorkReport.user_id = 当前登录用户）

9、✅ 已完成：团队人员这里默认是收起来的，然后可以点击展开，不然会影响页面观看效果，请完成这个功能，并且在报告历史里面要做一个区分，针对个人报告和团队报告分别用tag区分出来

- 实现内容：
  - 团队报告「成员成效明细」默认收起（仅显示折叠标题栏 + 已选摘要），点击标题栏展开/收起成员表格与全选/清空操作，避免影响页面观看效果
  - 报告历史新增类别 tag 区分：`work_reports` 表新增 `category` 字段（personal 个人 / team 团队 / group 工作组），团队报告卡片紫色「团队」tag、个人报告蓝色「个人」tag、工作组报告青色「工作组」tag，详情弹窗同步显示「团队报告 / 个人报告 / 工作组报告」
  - 启动时一次性幂等回填存量团队报告的 category（按标题特征匹配），历史团队报告不再误显示为「个人」

10、✅ 已完成：夜间模式板块目前显示存在问题，一旦打开后工作台的便签、归档查询的便签、人员管理的页面、便签管理的页面模板管理的页面都无法看清内容，颜色上需要调整到人眼可看的效果

- 实现内容：
  - 便签卡片（工作台/归档查询复用）夜间配色：原背景为内联样式硬编码浅色（#FEF3C7/#FEE2E2/#DBEAFE/#DCFCE7），改为 Tailwind 类 + `dark:` 深色变体（dark:amber-900/60、dark:red-900/60、dark:blue-900/60、dark:green-900/60），标题/正文/按钮/分隔线/反馈区均补充暗色文字与边框
  - 倒计时徽章、盯办/协作徽章加深底色提升白字对比度；「已归档」水印暗色下提亮
  - 人员管理：表头/表格行/姓名/用户名/部门/角色/状态徽章/操作按钮/预设组卡片/弹窗成员勾选列表等补齐 `dark:` 配色
  - 部门管理：部门树节点（展开箭头/选中高亮/hover/加号按钮）、部门详情卡片、新建部门弹窗补齐 `dark:` 配色
  - 便签管理（标签库）：标题、标签卡片元信息、使用次数补齐暗色配色
  - 模板管理：模板卡片、类型标签、编辑/删除按钮、新建/编辑/删除确认弹窗（含输入框、下拉、JSON 文本域）全部补齐 `dark:` 配色
  - 归档查询：时间轴视图切换、月份节点、恢复按钮、详情侧滑面板补齐暗色配色
  - 工作台：错误提示框、模板选择下拉补齐暗色配色

11、✅ 已完成：操作日志这里记录的数据太少了，还需要包括用户修改任务、删除任务、修改人员内容等等一系列的操作日志种类

- 实现内容：
  - 后端 `OperationLogger` 中间件 `parseOperation` 全面扩充操作类型解析，覆盖全量写接口：任务（创建/编辑/删除/完成归档/盯办/恢复/**反馈**/**上传附件**/**删除附件**）、标签、部门、用户（创建/更新/删除）、模板（创建/更新/**删除**）、工作组（创建/更新/**删除**/成员增改删/工作组创建任务/生成报告/删除报告）、预设组（创建/更新/删除）、成效报告（团队报告/AI报告/日报/周报/月报生成、删除报告、保存报告模板）、通知（全部已读/标记已读/删除）、聊天（发送消息/标记会话已读）、到期提醒确认、系统配置/AI配置（含**测试连通**）/配置文件
  - 修复资源 ID 提取：原实现把路径首段（如 `notes`）误作资源 ID，改为正确提取 URL 中的实体 ID
  - 丰富日志详情：成功操作不再统一覆盖为「成功」，改为「动作描述 · 关键字段摘要」（如「编辑任务 · title：xxx」「更新用户 · name：张三」）；失败操作自动追加 HTTP 状态码
  - 前端操作日志页：`actionLabels` 补全全部操作类型中文映射（含历史数据 create/update/delete 兜底翻译），表格新增「详情」列展示操作摘要（超长截断 + 悬浮全文）

12、✅ 已完成：现在内网访问页面非常卡顿，修改所有需要访问互联网资源的接口，如果能下载到本地，就直接从本地读取，而不是从互联网读取，避免页面访问卡顿，请优化前端页面。

- 实现内容：
  - 排查定位运行时唯一的互联网依赖：`style.css` 首行 `@import url('https://fonts.googleapis.com/css2?...')`（Google Fonts 在线字体），内网无外网时浏览器会等待字体请求导致页面卡顿/字体闪烁
  - 字体本地化：引入 `@fontsource/inter` 本地字体包（v5.3.0），在 `main.ts` 中 `import '@fontsource/inter/400/500/600/700.css'`，字体 woff2 经 Vite 打包进构建产物，彻底移除 Google Fonts 在线加载
  - 字体栈调整：中文字体回退链改为本地系统字体（Windows 微软雅黑 Microsoft YaHei / macOS 苹方 PingFang SC / Noto Sans CJK SC），移除不再加载的 Noto Sans SC
  - 验证：浏览器抓包确认页面加载 0 个外网请求（无 fonts.googleapis.com / gstatic），字体 100% 同源加载，Inter 渲染拉丁字符、中文回退系统字体，控制台无报错
  - Windows Server 2012 部署包已重新打包，本地字体（woff2）随 `frontend/assets/` 一并分发

13、✅ 已完成：系统上线后存在一个 bug，会让管理员权限的用户看到所有人的任务，这显然是不正确的，请排查并解决

- 根因：
  - `NoteRepository.List` 中 `super_admin` 角色分支为空实现 → 任务列表不加任何可见性过滤，系统管理员在工作台/归档查询看到所有人员的任务
  - 任务详情 `GET /notes/:id`、编辑、删除、导出、恢复、盯办等接口完全无访问校验，任何登录用户传任务 ID 即可越权访问他人任务
- 修复内容：
  - 任务列表可见性统一：除 `dept_admin`（仅本部门及子部门）外，`super_admin` / 组长 / 普通用户一律按「创建人 / 负责人 / 被指派人」过滤，管理员只能看到与自己相关的任务
  - 新增统一访问校验 `CheckUserAccess`（创建人/负责人/被指派人；部门管理员含本部门及子部门，与列表规则一致），应用于详情查看、编辑、删除、导出、恢复、盯办接口，越权统一返回 404（不泄露任务是否存在）
  - 修复过程中发现并消除详情接口与列表可见范围不一致的回归（部门管理员列表可见本部门任务、详情却 404 的问题）
- 验证：
  - 接口测试：admin（super_admin）列表不再包含普通用户任务，访问/编辑/删除/导出他人任务均 404；本人任务正常；dept_admin 列表与详情按部门一致；普通用户只可见本人任务
  - 浏览器验证：admin 工作台与归档仅显示自己相关任务，zhang 仅见自己的任务，控制台无报错

14、✅ 已完成：个人中心的热力图目前显示还存在问题，请重新设计并优化页面显示效果

- 根因：
  - 前端用 `toISOString()`（UTC）生成日期键，与后端本地时区 `DATE(completed_at)` 错位一天，数据匹配不上
  - 后端 `DATE(completed_at)` 经驱动序列化为 ISO 格式（如 `2026-05-07T00:00:00Z`），前端按 `YYYY-MM-DD` 匹配永远失败 → 热力图全空白
  - 列分组按「星期回退」切列，导致部分列累积超过 7 天、列高参差不齐错位
  - 固定浅色配色在暗色模式下刺眼
- 重新设计（GitHub 风格）：
  - **固定 7 行网格**（周一 → 周日），每列一周，1月1日所在周的周一为起点、12月31日所在周的周日为终点，首尾跨年格子透明占位，列整齐对齐
  - **本地时区日期键** `localDateStr`（与后端对齐，避免 UTC 偏移）；前端 `String(t.date).slice(0,10)` 兼容后端 ISO 格式
  - **后端日期规范化**：`TO_CHAR(completed_at, 'YYYY-MM-DD')` 返回纯日期（个人热力图与部门热力图同步修复）
  - **明暗双配色**：浅色 `#EFF4F9→#2563EB` 蓝系、暗色 `#1E293B→#93C5FD` 蓝系，暗色下不刺眼
  - 月份标签按列定位（含首列偏移）、星期标签固定 7 行、图例「少→多」动态适配暗色、悬停显示「日期 · N条归档」
- 验证：浏览器确认 2026 年 5/7、5/8、8/13 三个归档日正确显示蓝色色块、悬停提示正常、53 列×7 格整齐对齐、年份切换正常、明暗模式均可读、控制台无报错

  15.✅在侧边栏添加一个Bug反馈的板块，点击后则是Bug反馈的界面，每一个用户都可以创建问题，每一个问题点进去后要做成GitHub或Gitee的issues的格式，能够选择bug类型或者预期功能，每一个用户或者管理员能够在下面发言反馈，问题解决后就可以关闭这个issue。

- 实现（GitHub/Gitee Issues 风格）：
  - 侧边栏新增「🐛 Bug 反馈」入口，路由 `/issues`（列表页）+ `/issues/:id`（详情页）
  - 每个登录用户可创建问题：标题 + 详细描述 + 类型二选一（🐛 Bug 缺陷 / ✨ 预期功能），自动生成 `#1` `#2` 自增编号
  - 列表页支持状态筛选（全部/开放/已关闭）、类型筛选、关键词搜索（标题/内容）、分页、评论数统计、相对时间
  - 详情页 GitHub 风格：类型图标 + 标题 + #编号 + 类型/状态徽章、创建人信息条（头像/姓名/时间）、正文、评论区
  - 所有用户可在下面发言反馈；问题关闭后禁止继续评论（前后端一致）
  - 关闭权限：创建人本人或管理员（super_admin/dept_admin）可关闭/重新打开，其余用户 403
  - 操作日志新增 create_issue / add_issue_comment / update_issue_status 三种动作
  - 后端新增 `issues` 与 `issue_comments` 两张表，API：GET/POST /api/v1/issues、GET /api/v1/issues/:id、POST /api/v1/issues/:id/comments、PUT /api/v1/issues/:id/status
- 验证：接口测试全通过（创建 bug/feature、列表/筛选/搜索、评论、关闭权限 403/200、空标题 400）；浏览器验证侧边栏入口、新建弹窗、详情页 GitHub 风格、评论、关闭后评论禁用、暗色模式均正常

  16.在侧边栏添加一个聊天的板块，点击板块后则是聊天界面，聊天界面板块布局与模拟QQ、微信在线聊天的形式，可以选择好友进行聊天，聊天记录保存在数据库中，请详细设计页面风格和功能，并且支持表情包和emoji表情等功能，同时支持文件传输，传输的文件如果是图片，需要在聊天记录中显示图片，如果是其他文件，需要在聊天记录中显示文件名。
