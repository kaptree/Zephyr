# 新增需求

1、✅ 已完成：目前新建的便签任务，在进行编辑和创建的时候，不能进行回车和换行，并且没有富文本编辑功能。现在需要对便签编辑的任务进行优化，添加回车和换行功能，以及富文本编辑功能，并且编辑和创建便签的时候弹窗页面剧中进行，页面布局也更加美观和宽阔。

2、✅ 已完成：目前指派他人任务的时候不能对多人勾选，只能勾选单人，现在需要对指派他人任务的功能进行优化，添加对多人勾选的功能，并且针对一个小组的所有人能一键全部勾选。

3、✅ 已完成：指派他人任务的时候，没有反馈机制，指派的任务被完成后，被指派的人应该能对当前完成的工作进行反馈填报，并且通过系统通知的方式，通知指派任务的人。其中可能涉及到需要新建一个消息队列的机制，在系统的右上角显示通知的消息，通知的人可以点击消息，查看通知的内容。也可以点击每一个消息进去查看详情，这个可能要设计几张存储的表结构，并且补全用户之间聊天的功能，和消息推送系统提示音等一些列内容，请针对上述内容对项目进行设计方案并进行调整和测试。

- 实现内容：
  - 新增 `notifications`（通知）与 `chat_messages`（聊天）两张表结构，AutoMigrate 自动建表
  - 通知事件：任务指派 `task_assigned`、任务完成 `task_completed`、任务反馈 `task_feedback`、催办提醒 `task_remind`
  - 完成/反馈任务时被指派者填报反馈内容（富文本），系统自动通知任务发起人
  - 右上角通知铃铛：未读数角标、下拉通知列表、点击消息查看详情、可跳转对应任务、一键全部已读、单条删除
  - 用户间一对一聊天：会话列表（未读数）、消息记录、实时 WebSocket 推送、任务详情「联系」入口直达会话
  - 实时消息通道：`GET /ws/user/:user_id`（个人通知通道），新通知/新消息浏览器提示音（Web Audio 合成，无需外部资源）
  - 通知/聊天/反馈 API：`/notifications`、`/chat`、`/reminders`、`POST /notes/:id/feedback`
  - 修复：被指派者（assignee）可完成任务并提交反馈（原权限仅允许发起人完成）

4、✅ 已完成：通知中心这里有一个按钮可以跳转一个新页面可以查看所有通知内容，通知太多了就显示最近十条通知，其他通知就是到面板查看，并且当前的任务完成后的反馈内容需要添加到任务工作台的指派便签中，同时同步指派人和被指派人的便签内容和状态，通知中心的查看任务，无法将任务便签弹出来，请修复和完成上述内容，并做好测试工作

- 实现内容：
  - 新增「通知中心」独立页面 `/notifications`：展示全部通知（分页/加载更多、只看未读筛选、全部已读、单条删除、查看任务、返回工作台）
  - 铃铛下拉面板只显示最近 10 条通知，底部提供「查看全部通知 →」按钮跳转通知中心
  - 任务反馈同步到便签：任务列表/详情接口预加载 assignees（含 feedback_content），工作台「已完成」便签卡片显示绿色「任务反馈」区块（被指派人姓名 + 反馈内容），指派人与被指派人看到同一任务便签内容和状态
  - 修复「查看任务」无法弹出任务便签：改为监听 `route.query.note` 变化（同页面再次点击也能触发），归档任务通过详情接口归一化后打开详情面板

5、✅ 已完成：在系统管理的AI服务配置板块，需要新增一个支持dify的API调用的添加选项，并且针对添加的模型能够一键测试是否联通，连通后才能能保存并使用。

- 实现内容：
  - AI 服务商新增 `dify` 类型（openai/deepseek/qwen/zhipu/dify/custom），新增服务商下拉选择，自动填充名称与地址占位提示
  - 新增「一键测试连通」按钮与 `POST /api/v1/system/ai-configs/test` 接口（不落库）：dify 走 `GET {endpoint}/info` 探测（兼容带/不带 `/v1`，返回应用名），其他走 OpenAI 兼容 `POST /chat/completions`（max_tokens=1）
  - 测试结果展示：连通成功（绿色徽标 + 延迟 ms）/ 失败（红色徽标 + 错误信息）
  - 保存门禁：创建/更新 AI 配置前后端强制连通性测试，测试失败返回 400 拒绝保存；前端保存按钮在未通过测试时禁用
  - dify 业务调用：`POST /chat-messages`（blocking 模式），供 AI 报表/日报等功能复用；统一 `services.CallAIService` 双协议（OpenAI 兼容 / Dify）
  - AI 配置新增 `provider_type` 字段（旧数据归一化为 openai）

6、✅ 已完成：指派任务和完成指派任务后页面需要局部刷新一下，将最新的任务面板自动刷新出来，并且在指派任务的时候会有一个工作时间选项，任务下发后盯办任务的左上角就开始倒计时，任务时间不足的时候会通过通知的方式发送提醒。

- 实现内容：
  - 指派/协作任务表单新增「⏱ 工作时间」选项（不限制/1小时/2小时/4小时/8小时/1天/2天/3天/7天），后端按工作时间自动计算截止时间 `due_time`
  - 任务便签左上角实时倒计时：显示「剩余 X天X小时 / X小时X分钟 / 已超时 X」，每 30 秒自动刷新；剩余不足 1 小时或已超时显示红色警示，未完成的任务才显示（完成/归档后隐藏）
  - 到期提醒通知：新增后端调度器（`auto_remind_enabled` 开启，按 `auto_remind_interval_minutes` 间隔扫描），距截止不足 2 小时且未提醒过的未完成任务，向负责人/发起人/被指派人发送「任务到期提醒」通知（`task_due` 类型，WebSocket 实时推送，`due_remind_at` 字段去重）
  - 局部刷新：指派任务创建成功、任务完成归档（反馈提交）后自动刷新任务面板，最新任务/状态/倒计时立即展示

7、✅ 已完成：工作成效板块需要进行调整，添加一个团队报告的模块，通过团队报告模块，可以查看团队成员的工作成效，包括任务完成率、任务完成时间、任务完成数量等，并且可以按照选择时间范围生成周报。修改周报生成逻辑，生产按钮旁边新增一个模型选择，若无模型则是模板生产，若选择了模型，则使用选择的模型生成周报。

- 实现内容：
  - 工作成效页新增「👥 团队报告」Tab：时间范围选择（本周/本月/自定义日期），成员成效统计表（成员/部门/创建任务/完成任务/完成率/平均完成耗时/被盯办），顶部汇总卡片（成员数/创建数/完成数/整体完成率）
  - 权限范围：super_admin 查看全部部门成员，其他角色查看本部门（含子部门）成员
  - 团队统计接口 `GET /analytics/team-stats`：按时间范围聚合各成员任务完成率、平均完成耗时、完成数量、被盯办次数
  - 团队周报生成 `POST /analytics/team-report`：生成按钮旁新增 **「🤖 生成模型」选择下拉**（读取启用的 AI 配置，普通用户可访问 `GET /analytics/ai-configs` 简表接口）；未选择模型 → 模板生成（成员成效表格+总结）；选择模型 → 调用所选 AI 模型智能生成（失败自动回退模板）
  - 生成的团队报告自动保存至报告历史，支持复制/下载

8、✅ 已完成：团队周报这里每一个人可以勾选几个人设置为团队，不需要将每个人都拉进来，然后再针对勾选组建的团队可以生成报告，生成的报告属于当前用户。

- 实现内容：
  - 团队报告成员明细表新增勾选复选框：可自由连续勾选任意成员组建自定义团队（表格始终显示全部成员供勾选，勾选行蓝色高亮），并提供「全选 / 清空（全部成员）」快捷操作
  - 勾选后汇总卡片（团队成员数/创建任务/完成任务/整体完成率）实时按勾选成员本地过滤计算，提示「✅ 已勾选 X 人组建团队」
  - 生成周报/月报时携带 `user_ids`（`GET /analytics/team-stats` 的 `user_ids` 查询参数、`POST /analytics/team-report` 的 `user_ids` 字段），后端仅在当前用户可见成员范围内过滤，统计与报告只包含勾选成员
  - 未勾选任何成员时保持统计全部可见成员（原行为不变）
  - 生成的报告归属当前用户（WorkReport.user_id = 当前登录用户）

9、✅ 已完成：团队人员这里默认是收起来的，然后可以点击展开，不然会影响页面观看效果，请完成这个功能，并且在报告历史里面要做一个区分，针对个人报告和团队报告分别用tag区分出来

- 实现内容：
  - 团队报告「成员成效明细」默认收起（仅显示折叠标题栏 + 已选摘要），点击标题栏展开/收起成员表格与全选/清空操作，避免影响页面观看效果
  - 报告历史新增类别 tag 区分：`work_reports` 表新增 `category` 字段（personal 个人 / team 团队 / group 工作组），团队报告卡片紫色「团队」tag、个人报告蓝色「个人」tag、工作组报告青色「工作组」tag，详情弹窗同步显示「团队报告 / 个人报告 / 工作组报告」
  - 启动时一次性幂等回填存量团队报告的 category（按标题特征匹配），历史团队报告不再误显示为「个人」

10、✅ 已完成：夜间模式板块目前显示存在问题，一旦打开后工作台的便签、归档查询的便签、人员管理的页面、便签管理的页面模板管理的页面都无法看清内容，颜色上需要调整到人眼可看的效果

- 实现内容：
  - 便签卡片（工作台/归档查询复用）夜间配色：原背景为内联样式硬编码浅色（#FEF3C7/#FEE2E2/#DBEAFE/#DCFCE7），改为 Tailwind 类 + `dark:` 深色变体（dark:amber-900/60、dark:red-900/60、dark:blue-900/60、dark:green-900/60），标题/正文/按钮/分隔线/反馈区均补充暗色文字与边框
  - 倒计时徽章、盯办/协作徽章加深底色提升白字对比度；「已归档」水印暗色下提亮
  - 人员管理：表头/表格行/姓名/用户名/部门/角色/状态徽章/操作按钮/预设组卡片/弹窗成员勾选列表等补齐 `dark:` 配色
  - 部门管理：部门树节点（展开箭头/选中高亮/hover/加号按钮）、部门详情卡片、新建部门弹窗补齐 `dark:` 配色
  - 便签管理（标签库）：标题、标签卡片元信息、使用次数补齐暗色配色
  - 模板管理：模板卡片、类型标签、编辑/删除按钮、新建/编辑/删除确认弹窗（含输入框、下拉、JSON 文本域）全部补齐 `dark:` 配色
  - 归档查询：时间轴视图切换、月份节点、恢复按钮、详情侧滑面板补齐暗色配色
  - 工作台：错误提示框、模板选择下拉补齐暗色配色

11、✅ 已完成：操作日志这里记录的数据太少了，还需要包括用户修改任务、删除任务、修改人员内容等等一系列的操作日志种类

- 实现内容：
  - 后端 `OperationLogger` 中间件 `parseOperation` 全面扩充操作类型解析，覆盖全量写接口：任务（创建/编辑/删除/完成归档/盯办/恢复/**反馈**/**上传附件**/**删除附件**）、标签、部门、用户（创建/更新/删除）、模板（创建/更新/**删除**）、工作组（创建/更新/**删除**/成员增改删/工作组创建任务/生成报告/删除报告）、预设组（创建/更新/删除）、成效报告（团队报告/AI报告/日报/周报/月报生成、删除报告、保存报告模板）、通知（全部已读/标记已读/删除）、聊天（发送消息/标记会话已读）、到期提醒确认、系统配置/AI配置（含**测试连通**）/配置文件
  - 修复资源 ID 提取：原实现把路径首段（如 `notes`）误作资源 ID，改为正确提取 URL 中的实体 ID
  - 丰富日志详情：成功操作不再统一覆盖为「成功」，改为「动作描述 · 关键字段摘要」（如「编辑任务 · title：xxx」「更新用户 · name：张三」）；失败操作自动追加 HTTP 状态码
  - 前端操作日志页：`actionLabels` 补全全部操作类型中文映射（含历史数据 create/update/delete 兜底翻译），表格新增「详情」列展示操作摘要（超长截断 + 悬浮全文）

12、✅ 已完成：现在内网访问页面非常卡顿，修改所有需要访问互联网资源的接口，如果能下载到本地，就直接从本地读取，而不是从互联网读取，避免页面访问卡顿，请优化前端页面。

- 实现内容：
  - 排查定位运行时唯一的互联网依赖：`style.css` 首行 `@import url('https://fonts.googleapis.com/css2?...')`（Google Fonts 在线字体），内网无外网时浏览器会等待字体请求导致页面卡顿/字体闪烁
  - 字体本地化：引入 `@fontsource/inter` 本地字体包（v5.3.0），在 `main.ts` 中 `import '@fontsource/inter/400/500/600/700.css'`，字体 woff2 经 Vite 打包进构建产物，彻底移除 Google Fonts 在线加载
  - 字体栈调整：中文字体回退链改为本地系统字体（Windows 微软雅黑 Microsoft YaHei / macOS 苹方 PingFang SC / Noto Sans CJK SC），移除不再加载的 Noto Sans SC
  - 验证：浏览器抓包确认页面加载 0 个外网请求（无 fonts.googleapis.com / gstatic），字体 100% 同源加载，Inter 渲染拉丁字符、中文回退系统字体，控制台无报错
  - Windows Server 2012 部署包已重新打包，本地字体（woff2）随 `frontend/assets/` 一并分发

13、✅ 已完成：系统上线后存在一个 bug，会让管理员权限的用户看到所有人的任务，这显然是不正确的，请排查并解决

- 根因：
  - `NoteRepository.List` 中 `super_admin` 角色分支为空实现 → 任务列表不加任何可见性过滤，系统管理员在工作台/归档查询看到所有人员的任务
  - 任务详情 `GET /notes/:id`、编辑、删除、导出、恢复、盯办等接口完全无访问校验，任何登录用户传任务 ID 即可越权访问他人任务
- 修复内容：
  - 任务列表可见性统一：除 `dept_admin`（仅本部门及子部门）外，`super_admin` / 组长 / 普通用户一律按「创建人 / 负责人 / 被指派人」过滤，管理员只能看到与自己相关的任务
  - 新增统一访问校验 `CheckUserAccess`（创建人/负责人/被指派人；部门管理员含本部门及子部门，与列表规则一致），应用于详情查看、编辑、删除、导出、恢复、盯办接口，越权统一返回 404（不泄露任务是否存在）
  - 修复过程中发现并消除详情接口与列表可见范围不一致的回归（部门管理员列表可见本部门任务、详情却 404 的问题）
- 验证：
  - 接口测试：admin（super_admin）列表不再包含普通用户任务，访问/编辑/删除/导出他人任务均 404；本人任务正常；dept_admin 列表与详情按部门一致；普通用户只可见本人任务
  - 浏览器验证：admin 工作台与归档仅显示自己相关任务，zhang 仅见自己的任务，控制台无报错

14、✅ 已完成：个人中心的热力图目前显示还存在问题，请重新设计并优化页面显示效果

- 根因：
  - 前端用 `toISOString()`（UTC）生成日期键，与后端本地时区 `DATE(completed_at)` 错位一天，数据匹配不上
  - 后端 `DATE(completed_at)` 经驱动序列化为 ISO 格式（如 `2026-05-07T00:00:00Z`），前端按 `YYYY-MM-DD` 匹配永远失败 → 热力图全空白
  - 列分组按「星期回退」切列，导致部分列累积超过 7 天、列高参差不齐错位
  - 固定浅色配色在暗色模式下刺眼
- 重新设计（GitHub 风格）：
  - **固定 7 行网格**（周一 → 周日），每列一周，1月1日所在周的周一为起点、12月31日所在周的周日为终点，首尾跨年格子透明占位，列整齐对齐
  - **本地时区日期键** `localDateStr`（与后端对齐，避免 UTC 偏移）；前端 `String(t.date).slice(0,10)` 兼容后端 ISO 格式
  - **后端日期规范化**：`TO_CHAR(completed_at, 'YYYY-MM-DD')` 返回纯日期（个人热力图与部门热力图同步修复）
  - **明暗双配色**：浅色 `#EFF4F9→#2563EB` 蓝系、暗色 `#1E293B→#93C5FD` 蓝系，暗色下不刺眼
  - 月份标签按列定位（含首列偏移）、星期标签固定 7 行、图例「少→多」动态适配暗色、悬停显示「日期 · N条归档」
- 验证：浏览器确认 2026 年 5/7、5/8、8/13 三个归档日正确显示蓝色色块、悬停提示正常、53 列×7 格整齐对齐、年份切换正常、明暗模式均可读、控制台无报错

  15.✅在侧边栏添加一个Bug反馈的板块，点击后则是Bug反馈的界面，每一个用户都可以创建问题，每一个问题点进去后要做成GitHub或Gitee的issues的格式，能够选择bug类型或者预期功能，每一个用户或者管理员能够在下面发言反馈，问题解决后就可以关闭这个issue。

- 实现（GitHub/Gitee Issues 风格）：
  - 侧边栏新增「🐛 Bug 反馈」入口，路由 `/issues`（列表页）+ `/issues/:id`（详情页）
  - 每个登录用户可创建问题：标题 + 详细描述 + 类型二选一（🐛 Bug 缺陷 / ✨ 预期功能），自动生成 `#1` `#2` 自增编号
  - 列表页支持状态筛选（全部/开放/已关闭）、类型筛选、关键词搜索（标题/内容）、分页、评论数统计、相对时间
  - 详情页 GitHub 风格：类型图标 + 标题 + #编号 + 类型/状态徽章、创建人信息条（头像/姓名/时间）、正文、评论区
  - 所有用户可在下面发言反馈；问题关闭后禁止继续评论（前后端一致）
  - 关闭权限：创建人本人或管理员（super_admin/dept_admin）可关闭/重新打开，其余用户 403
  - 操作日志新增 create_issue / add_issue_comment / update_issue_status 三种动作
  - 后端新增 `issues` 与 `issue_comments` 两张表，API：GET/POST /api/v1/issues、GET /api/v1/issues/:id、POST /api/v1/issues/:id/comments、PUT /api/v1/issues/:id/status
- 验证：接口测试全通过（创建 bug/feature、列表/筛选/搜索、评论、关闭权限 403/200、空标题 400）；浏览器验证侧边栏入口、新建弹窗、详情页 GitHub 风格、评论、关闭后评论禁用、暗色模式均正常

  16.✅在侧边栏添加一个聊天的板块，点击板块后则是聊天界面，聊天界面板块布局与模拟QQ、微信在线聊天的形式，可以选择好友进行聊天，聊天记录保存在数据库中，请详细设计页面风格和功能，并且支持表情包和emoji表情等功能，同时支持文件传输，传输的文件如果是图片，需要在聊天记录中显示图片，如果是其他文件，需要在聊天记录中显示文件名。

- 实现（仿 QQ/微信在线聊天）：
  - 侧边栏新增「💬 聊天」入口，路由 `/chat` 全屏聊天页，左右布局：左侧栏（会话 / 通讯录双 tab）+ 右侧消息主区
  - 通讯录 tab 展示全量用户（姓名+部门，搜索过滤，**在线用户优先排序**），点击即发起会话；会话 tab 展示最近会话（头像/姓名/最后消息摘要/时间/未读角标）；通讯录与会话列表均显示账号在线状态（头像右下角绿点 + 在线/离线文字，实时刷新）
  - 消息区气泡分左右（自己蓝色右侧、对方白色左侧），支持加载更早历史（滚动顶部翻页）
  - 表情包 + emoji：输入区「😊」弹出 emoji 面板（表情包/笑脸/手势/动物/食物/活动/旅行/物品/符号 9 个分类），点击插入输入框
  - 文件传输：输入区「📎」选择文件（≤10MB，支持绝大部分格式——压缩包/文档/表格/音视频/字体等，仅拦截可执行/脚本/网页等危险文件 exe/bat/ps1/html/svg 等），图片消息在聊天记录中直接显示图片（点击可放大预览），其他文件显示文件卡片（图标+文件名+大小+点击下载）
  - 文件白名单/黑名单可配置：系统管理 →「📎 聊天文件」Tab 由管理员编辑（逗号分隔扩展名），保存后**热加载立即生效**无需重启（存 chat_file_policies 表，接口 GET/PUT /api/v1/system/chat-file-policy，仅 super_admin 可访问）；规则：黑名单优先，白名单非空时仅允许白名单内格式，白名单为空时除黑名单外全部放行
  - 聊天记录落库 `chat_messages`（新增 type 字段：text/image/file + file_name/file_path/file_size/mime_type），WS 实时推送新消息
  - 会话接口返回好友姓名/头像、图片/文件摘要（[图片] / [文件] xx）；新增聊天独立上传接口 `POST /api/v1/chat/attachments`
  - 顶栏「消息聊天」抽屉（ChatDrawer）同步支持图片/文件消息渲染
  - 操作日志新增 `upload_chat_attachment`（发送聊天文件）
- 验证：接口测试全通过（文本+emoji、图片上传+图片消息、PDF 上传+文件消息、非法类型 400、会话含 peer_name/摘要、消息历史、标记已读）；浏览器实测 admin 发文本/emoji、zhang 收到并查看图片与文件消息、实时同步、未读角标清零、暗色模式均正常

  17.✅对于指派别人干活的任务，在任务标签的右上角标注“指派”而不是“盯办1”，同时移除工作台中每一个任务卡片的盯办按钮，因为对自己的任务进行盯办没有意义，所以移除。再新增两个按钮，分别为“重要”和“删除”按钮，点击“重要”按钮后，任务卡片会显示为红色，点击“删除”按钮后，任务卡片会从工作台中移除。

- 实现（StickyNoteCard.vue / WorkbenchPage.vue）：
  - 任务卡片右上角徽章：`source_type === 'assigned'` 的指派任务按视角显示——发起者（创建人）见蓝色「指派」徽章、接收者见红色「盯办N」徽章（详见需求 18）；非指派的红色（重要）任务显示「盯办N」
  - 移除工作台任务卡片与详情面板中的「盯办」按钮（对自己的任务盯办无意义）
  - 新增「重要」按钮（非红色卡片显示，`extraActions` 控制）：点击后调用更新接口将 `color_status` 置为 `red`，卡片立即变为红色；详情面板同步提供「重要」按钮
  - 新增「删除」按钮（`extraActions` 控制）：点击弹出确认框「确定删除任务...删除后将从工作台移除，可在归档中恢复」，确认后软删除，卡片从工作台消失，可在归档中恢复
  - 专项工作组详情页（GroupDetailPage）与归档页（ArchivePage）不显示新增按钮（避免未绑定事件的无效按钮）；`extraActions` 默认 false
- 验证：浏览器实测 admin 账号工作台 16 张指派任务右上角均为「指派」徽章、卡片与详情面板无「盯办」按钮、「重要」点击后卡片变红、「删除」确认后卡片从工作台消失、夜间模式显示正常；`vue-tsc` 构建通过

  18.✅对于指派的任务，任务的发起者的任务卡片右上角显示的是“指派”，任务卡片用浅蓝色背景，对于接收任务的账号看到的就是“盯办”，任务卡片用红色背景，同时在任务卡片的右上角标注“盯办”。并且点击任务详情后创建人部分是当前账号，负责人则是选择的任务接收任务的账号。

- 实现（StickyNoteCard.vue / WorkbenchPage.vue / types / stores）：
  - 卡片按视角区分：`source_type === 'assigned'` 且当前账号 = `creator_id`（发起者/创建人）→ 卡片浅蓝色背景 + 右上角蓝色「指派」徽章；否则（接收者）→ 卡片红色背景 + 右上角「盯办N」徽章（N 为盯办提醒次数）
  - 非指派任务被标记「重要」（color_status='red'）仍显示红色 + 「盯办N」；协作任务仍为蓝色 + 「协作」
  - 详情面板：标题旁徽章同步区分（发起者「指派」浅蓝标签 / 接收者「盯办中」红色标签）；信息区新增「创建人」行（显示创建人姓名，发起者视角即当前账号），「负责人」展示被指派账号（assignees，含发起者 initiator 角色）
  - 前端 `Note` 类型与 normalize 增加 `creator` / `owner` 字段（后端 List/FindByID 已 Preload("Creator")/Preload("Owner")，json: creator/owner）
- 验证：浏览器实测 admin（发起者）看指派任务浅蓝 +「指派」、详情创建人=张局长/负责人=张局长+张三；zhang（接收者）看同一任务红色 +「盯办1」、详情徽章「盯办中」；夜间模式双视角显示正常；`vue-tsc` 构建通过

  19.✅针对目前任务指派之后需要了解大家签收的情况，需要添加一个sign_status字段，用于记录任务是否被签收。签收后在任务卡片的右上角标注“已签收”。同时在任务分发账号处能看到哪些人签收了任务。请设计这一套签收体系。

- 实现（后端 + 前端）：
  - 数据模型：`note_assignees` 表新增 `sign_status`（unsigned 未签收 / signed 已签收，默认 unsigned）与 `signed_at`（签收时间）字段（AutoMigrate 自动加列，存量数据默认未签收）
  - 签收接口：`POST /api/v1/notes/:id/sign`，仅「指派任务（source_type=assigned）」的被指派人（assignees 中非发起者 initiator 角色）可签收，重复签收幂等返回；签收后置本人行 sign_status=signed + signed_at，写操作日志 `sign_note`（任务签收），并通知任务发起人「任务已签收」
  - 接收者视角：未签收卡片右上角红色「盯办N」；签收后变为绿色「已签收」徽章（红色卡片背景不变）；详情面板底部操作区上方出现蓝色「签收任务」按钮（发起者不可签收自己的指派任务，不显示该按钮）
  - 分发账号视角：卡片右上角蓝色「指派」徽章不变，卡片底部显示签收统计「已签收 x/y / 已全部签收」（悬停 title 显示已签收人姓名，统计排除发起者本人）；详情面板「负责人」区每个被指派人姓名旁显示签收徽标（绿色「已签收」+ 签收时间 / 灰色「未签收」）
  - 操作日志页面新增 `sign_note` →「任务签收」映射
- 验证：接口测试（zhang 签收 200 且 signed_at 落库、重复签收幂等 200、发起者 admin 签收 403）；浏览器实测 zhang 未签收显示「盯办1」→ 点「签收任务」→ 绿色「已签收」且刷新持久化；admin 看卡片「已全部签收/已签收 x/y」、悬停显示「已签收：张三」、详情负责人区区分已签收/未签收；夜间模式正常；`vue-tsc` 构建通过

  20.✅对于创建的任务无论是否指派，都可以将任务进行抄送，可以多选抄送人将当前内容抄送一份给抄送人。抄送人的任务卡片用紫色的背景，同时在任务卡片的右上角标注“抄送”。

- 实现（后端 + 前端）：
  - 数据模型：新增 `note_ccs` 表（note_id + user_id + user + created_at，AutoMigrate 注册），`Note` 关联 `ccs` 字段
  - 创建接口：`CreateNoteRequest` 新增 `cc_user_ids`（可多选，去重、排除发起者本人），无论 source_type 均可抄送；创建后向每位抄送人发送 `task_cc` 通知「您收到任务抄送」
  - 列表与详情可见性：`List`（dept_admin 与默认分支）与 `FindByID` 增加抄送人可见条件并预加载 ccs.user；`CheckUserAccess`（查看）含抄送人
  - 权限：抄送人可查看 + 完成/删除（后续补充需求），但编辑/盯办/签收仍仅限参与者——`CanManage`/`CheckParticipantAccess`（排除抄送人）用于 PUT/remind/sign 拦截（403「您仅被抄送该任务，无权执行此操作」）
  - 抄送人视角：任务卡片**紫色背景**（日间 purple-100 / 夜间 purple-900/60）+ 右上角紫色「抄送」徽章 + 紫色「任务抄送」来源标签；卡片操作区提供「完成并归档」「删除」两个按钮；详情面板标题旁紫色「抄送」标签，底部操作区为「完成并归档 / 删除 / 关闭」（无保存/重要/签收）
  - 发起者视角：卡片与操作区正常，发起者自己不被标记为抄送；同时被指派与抄送的用户按参与者视角显示
  - 创建模态框：新增「抄送人员」多选选择器（UserPicker，对所有任务类型可用），提交携带 cc_user_ids
  - 通知中心/铃铛：新增 `task_cc`（任务抄送/紫色）、`task_signed`（任务签收/青色）类型标签与配色
- 补充（来源标注 + 抄送任务按钮）：
  - 每张任务卡片标题上方新增**来源标签**（按视角）：自己创建（灰色）/ 自己指派（蓝色，发起者看指派任务）/ 上级指派（橙色，接收者看指派任务）/ 任务抄送（紫色，抄送人）/ 协同任务（青色）
  - 抄送任务卡片与详情操作区提供「完成并归档」「删除」两个按钮（无「重要」）；抄送人点击「完成并归档」直接归档（不弹反馈填报）；后端放开抄送人的完成/删除权限（编辑/盯办仍仅限参与者）
- 验证：接口测试（admin 创建抄送任务 ccs 落库、zhang 列表可见且详情 200、zhang 删除/完成均 403、admin 删除 200）；浏览器实测 admin 创建多选抄送 2 人、zhang 见紫色卡片 +「抄送」徽章且无操作按钮、详情仅「关闭」、admin 见普通卡片且自己不标记为抄送、夜间模式正常；`vue-tsc` 构建通过

  21.✅ 已完成：目前的角色划分这里还差一个角色就是公司领导，对于公司领导来说权限大于部门管理员，又不需要管理系统配置与日志相关操作，同时对于公司领导这个角色来说单独有一个板块可以用户查看对应用户的工作台内容，请针对这个角色进行设计，并做好与其他角色权限的管理与分配。

- 设计定位：**公司领导（company_leader）** 介于系统管理员与部门管理员之间——全局数据范围（可见全公司任务），但不需要系统配置与操作日志管理；删除性操作（删除用户/部门/模板）仍归系统管理员独占
- 权限矩阵（auth_service `getPermissions`）：
  - 任务：create_note_self / create_note_assigned / edit_others_note / delete_note / remind / view_all_archive（全局归档）/ view_dept_archive / view_group_archive
  - 管理：manage_departments（部门增改，不含删除）/ manage_users（用户增改，不含删除）/ manage_tags / manage_templates（模板增改，不含删除）
  - 大屏与指令：access_screen / send_command
  - 专属：**inspect_user_workbench**（用户工作台查看）
  - 无：manage_system（系统配置、操作日志、AI 配置、聊天文件策略等 /system 接口仍为 super_admin 独占，company_leader 访问返回 403）
- 用户工作台板块：
  - 新增页面 `/workbench/inspect`（InspectWorkbenchPage）：顶部用户搜索选择器（按姓名/部门搜索、点击外部关闭）、状态筛选（待办含总数 / 已完成 / 已归档）、任务卡片网格（来源标签、重要红标、创建人、负责人 + 已签收徽标）
  - 新增接口 `GET /api/v1/notes/users/:userId/workbench?status=active|completed|archived`（RequireRoles super_admin + company_leader，返回分页结构）
  - 数据口径：目标用户作为创建人/负责人/被指派人/抄送人任一维度即可命中；`checkAccess` 中 company_leader 短路放行全公司任意任务详情
- 路由放权：departments 增改、users 增改、rooms 指令、presets 增改、ledger/stats 均追加 company_leader；`DELETE`（users/departments/presets）与 `system` 组保持 super_admin 独占
- 前端：Role 类型 / Permission 类型新增值、auth store 新增 isCompanyLeader、侧边栏「🔍 用户工作台」菜单（permission 过滤天然隐藏于无权限角色）、个人中心/人员管理/操作日志角色文案与徽章色补充（公司领导 = 紫色徽章）
- 验证：接口测试（leader1 登录后 inspect 接口 200 返回 zhang 任务、system/config 与 system/operations 403、创建用户/部门 201、删除用户 403；dept_admin 调 inspect 403）；浏览器实测（leader1 侧边栏含「用户工作台」且无「系统管理/操作日志」、选择张三加载 9 条待办含来源/已签收徽标、状态筛选切换正常、个人中心角色显示「公司领导」；admin 含用户工作台 + 系统管理 + 操作日志；wang 三者均无）；`vue-tsc` 构建通过

  22.✅ 已完成：当前根目录下存在一个biaoqing目录，里面存放的是三种类型的表情包，现在需要将这些表情包加入到聊天页面中，并且每一个用户可以自己上传自己的表情包，对于系统管理员可以批量上传新的表情包（通过文件夹或者单个文件的方式）。

- 系统内置表情：biaoqing/ 三套表情（熊猫头 100 / 狗狗 99 / 猫猫 100）复制到 `uploads/emoticons/system/{分类}/`，后端启动时幂等扫描导入 `emoticons` 表（`EnsureSystemEmoticons`，重复启动不重复入库）
- 数据模型 `emoticons`：name / category（分类）/ path（/uploads/emoticons/...）/ uploader_id（空=系统内置）/ is_system
- 接口：
  - `GET /api/v1/emoticons?category=` 表情列表（分类列表 + 表情；系统内置全部 + 当前用户上传的「我的表情」，他人上传的不可见）
  - `POST /api/v1/emoticons` 单文件上传（任意登录用户 → 「我的表情」分类，存 `uploads/emoticons/user/{uid}/`）
  - `POST /api/v1/emoticons/batch` 批量上传（仅 super_admin；支持 multiple 多选文件或 webkitdirectory 选文件夹——文件夹名作为分类，多文件默认分类「默认表情包」）
  - `DELETE /api/v1/emoticons/:id` 删除（自己的可删；系统内置仅 super_admin 可删）
- 前端聊天：
  - EmojiPicker 表情面板新增图片表情分类 tab（熊猫头/狗狗/猫猫/我的表情，位于「表情包」之后，懒加载按需拉取），网格缩略图，点击直接作为图片消息发送（`POST /chat/:id/messages` type=image）
  - 底部工具条：「➕ 上传表情」（所有用户）；「🗂 批量上传」（仅管理员，支持文件夹/多选）
  - 悬停自己的表情显示红色「×」删除按钮
- 部署：build-windows2012.sh 将 `uploads/emoticons/` 打进部署包 backend/uploads/；新部署启动后自动导入 DB
- 验证：接口测试（列表 299 个含 3 分类、熊猫头 100 个、图片可访问 200、zhang 上传成功见「我的表情」、dept_admin 批量 403、admin 批量 3 个成功、删除成功）；浏览器实测（zhang 聊天表情面板 3 个图片分类、点击熊猫头 001 作为图片消息发送成功、上传 050.gif 后出现「我的表情」tab、悬停显示删除按钮；admin 面板有「🗂 批量上传」而 zhang 无）；`vue-tsc` 构建通过

  23.目前的模版管理部分应该开放给所有创建，包括系统管理员、部门管理员、公司领导、普通用户，并且每一个创建的模版都可以被其他用户查看与使用，但是不能被删除和修改，当然系统管理员拥有所有编辑和删除的权限。模版的卡片上面需要标注模版创建人和模版简介，模版内容是类似任务记录/报告编辑的模版，例如针对不同类型的任务，需要不同的模版内容，而不是json格式模板。并且需要新增一个搜索框对模板进行搜索

- ✅ 已完成
- 实现内容：
  - **开放创建与共享**：模板接口本就对所有登录用户开放；前端侧边栏「模板管理」与路由移除 `manage_templates` 权限限制 → 系统管理员/部门管理员/公司领导/普通用户均可进入创建，所有用户可查看与使用全部模板
  - **改删权限**：后端新增 `canManageTemplate` —— 系统管理员拥有全部模板的编辑/删除权限；其余用户仅可编辑/删除自己创建的模板（他人模板 PUT/DELETE 返回 403「仅模板创建者或系统管理员可以…」）；系统内置模板仅系统管理员可改删
  - **卡片标注**：`Template` 模型新增 `description`（简介）、`content`（模板正文）字段与 `Creator` 关联（列表/详情预加载创建人）；卡片展示创建人姓名、模板简介、内容预览（旧 JSON 字段模板自动兼容预览为「【字段名】」）
  - **内容格式**：新建/编辑模板改为填写「模板简介 + 模板正文」（纯文本任务记录/报告模板，如【任务背景】【任务目标】【完成标准】），移除原 JSON 字段定义与布局选择 UI；`WorkbenchPage` 创建任务选用模板时把模板正文直接填入内容编辑器（旧模板字段名转正文兼容）
  - **搜索**：后端 `GET /api/v1/templates?keyword=` 按名称/简介/内容模糊搜索；模板管理页新增搜索框 + 清空
- 验证：接口测试（admin/wang/zhang 三角色均可创建、互相可见含创建人/简介/内容、zhang 改删他人 403、zhang 改删自己 200、admin 改删任意 200、关键词搜索生效）；浏览器实测（zhang 普通用户侧边栏可见模板管理、卡片显示创建人/简介/内容、他人模板无编辑删除按钮显示「仅创建者/管理员可编辑」、自己模板可编辑删除、搜索过滤+清空恢复；admin 所有卡片均有编辑/删除按钮；创建任务选择模板后内容自动填入）；`go build` / `vue-tsc` 通过

  24.目前聊天消息和通知消息都太不明显了，需要有一个动画弹窗，从右上角弹出来，不然用户无法看到，对于离线的用户登录上线后消息会逐步从右上角弹出来，点击后可以跳转聊天对话页面，对于任务消息点击后也可以跳转任务详情页面。弹出动画要流畅丝滑。

- ✅ 已完成
- 实现内容：
  - 新增全局弹窗组件 `FloatingPopups.vue`（挂在 App.vue）：右上角白色卡片（💬 聊天 / 🔔 通知图标 + 标题 + 内容预览 + 时间 + 关闭 ×），点击跳转
  - 弹窗队列：`notification store` 新增 `popups`/`popupQueue`/`enqueuePopup`/`dismissPopup`——同时最多展示 2 条，队列按 700ms 间隔逐步弹出，每条展示 5 秒自动消失
  - 实时触发：`handleNewMessage` 收到对方聊天消息弹窗（自己发送/正在查看的会话不弹）；`handleNewNotification` 收到任务通知弹窗
  - 离线补推：WebSocket `onopen` 时 `replayMissed()`——拉取最近未读通知（≤5）与各未读会话的未读消息（≤5，每会话 ≤3），从旧到新逐步从右上角弹出
  - 点击跳转：聊天弹窗 → `/chat?peer=对方id`（`ChatPage` 新增 `?peer` 查询支持，挂载/查询变化时自动打开对应会话）；任务通知弹窗 → `/workbench?note=任务id`（复用既有任务详情弹窗机制）；其他 → 通知中心
  - 动画：`cubic-bezier(0.34, 1.56, 0.64, 1)` 弹性缓动从右侧滑入（scale 0.9→1 带回弹），消失 0.25s 平滑退出
- 验证：浏览器实测（工作台收到 wang 消息 → 右上角弹出 💬「王大队 + 消息预览」→ 点击跳转 /chat?peer= 并自动打开会话；wang 指派任务 → 弹出 🔔「您有新任务」→ 点击跳转 /workbench?note= 并弹出任务详情面板；动画运行时采样 translateX 384→0/scale 0.9→1.01 带弹性回弹；控制台无报错）；`vue-tsc` 通过

  25.工作成效分析的时候在进行报告生成后的报告历史里面后点击后应该跳转到子页面，能够更加清楚的看到工作报告，并且按照时间顺序依次将每一个人的任务内容和反馈情况同时附在报告的下方，整体以报告文章的形式展示，同时可以下载，支持pdf和word的格式。

- ✅ 已完成
- 实现内容：
  - **子页面跳转**：报告历史卡片点击由弹窗改为跳转 `/analytics/reports/:id` 详情子页面（新增 `ReportDetailPage.vue` 路由）；移除原报告详情弹窗
  - **报告文章展示**：子页面以文章形式展示——大标题、生成人/类别/周期/生成时间元信息、统计周期、Markdown 正文富文本渲染
  - **成员任务明细**：后端 `WorkReport` 新增 `Detail`（jsonb）列，在报告生成时采集周期内各成员创建的任务（标题/内容/状态/创建与完成时间）与关联反馈（"姓名：内容"，多条换行）快照；团队报告按成员汇总、任务按时间正序；新增 `GET /api/v1/analytics/reports/:id/detail` 接口返回报告+明细；页面在报告下方展示「📋 成员任务明细」板块（成员头像+姓名+任务数，任务卡片含时间/状态徽章/内容/绿色任务反馈区）
  - **导出下载**：顶部「📄 导出 Word」（HTML .doc 文档下载，含正文+成员明细）与「🖨 导出 PDF」（新窗口渲染文章后调用浏览器打印，可另存为 PDF）；保留复制/删除/返回
  - **兼容**：旧报告（无明细）打开显示「该报告暂无成员任务明细」提示，不崩溃（后端 members 空数组兜底 + 前端 members 数组判空）
- 验证：接口测试（个人周报明细含 7 项任务按时间排序、团队周报 16 成员含任务数、任务反馈正确附带「王大队：已完成测试反馈内容」）；浏览器实测（点击报告卡片跳转 /analytics/reports/:id、文章形式展示、成员任务明细含内容+绿色反馈并按时间排序、Word 下载触发 .doc、PDF 打开打印窗口、返回正常、旧报告打开正常无 TypeError）；`go build` / `vue-tsc` 通过

  26.在bug反馈这个模块，可以添加一个订阅按钮，一旦有人新建issue就会给订阅人发送消息提示，针对每一个新增的Issue，创建issue的人你会收到每一个评论的消息提示，对于在issue回复的账号来说，一旦在某一个issue进行回复，也会一直收到这个issue下新增评论的消息提示

- ✅ 已完成
- 实现内容：
  - **订阅模型**：新增 `issue_subscribers` 表（issue_id + user_id 复合主键）；创建 issue 时创建人**自动订阅**；评论时评论者**自动订阅**（持续接收该 issue 后续新评论通知）；也可在详情页手动订阅/取消订阅
  - **接口**：`POST/DELETE /api/v1/issues/:id/subscribe`（订阅/取消，返回订阅状态与人数）；`GET /api/v1/issues/:id` 详情返回当前用户 `subscribed` 与 `subscriber_count`
  - **评论通知**：新增评论时向该 issue 全部订阅人（排除评论者自己）发送 `issue_comment` 通知；`Notification` 模型新增 `issue_id` 关联与 `NotifyIssue` 服务方法；通知通过 WebSocket 实时推送 + 写入通知中心（通知铃铛/未读数/右上角弹窗复用需求24机制）
  - **前端**：Issue 详情页新增「🔔 订阅 / 🔕 已订阅（人数）」按钮，点击切换；评论后自动刷新为已订阅；右上角 issue_comment 弹窗点击跳转 `/issues/:id` 问题详情
  - **显示名**：issue 创建人/评论人/通知内容统一使用用户显示姓名（如「孙队」而非登录名「sun」）
- 验证：接口测试（wang 创建 issue 自动订阅 count=1、admin/sun 评论后自动订阅 count=3、wang 收到 2 条 issue_comment 通知含 issue_id、admin 收到 1 条且自己评论不通知自己、取消/重新订阅正常）；浏览器实测（订阅按钮切换与人数实时变化、评论后自动变为已订阅、wang 评论后右上角弹窗出现「【Issue #5】…有新评论」、点击弹窗跳转问题详情、取消订阅/重新订阅成功无报错、通知显示评论人显示姓名）；`go build` / `vue-tsc` 通过

  27.Bug反馈部分，默认切换到开放的issue，并且关闭的issue的帖子在列表上用浅灰色背景。

- ✅ 已完成
- 实现内容：
  - **默认开放**：Bug 反馈列表页状态筛选默认值为「开放中」（`statusFilter` 初值由 `''` 改为 `'open'`），首次进入/刷新页面默认只展示开放的 issue
  - **浅灰背景**：列表行按状态区分——已关闭的 issue 使用浅灰背景（`bg-slate-100`，暗色模式 `dark:bg-slate-700/40`），开放 issue 保持白底；悬停态也分别适配，两种状态行视觉区分明显
- 验证：浏览器实测（默认选中「🟢 开放中」高亮且列表仅含开放 issue、刷新后状态保持；「已关闭」tab 下关闭行背景 `rgb(241,245,249)` 浅灰；「全部」tab 下开放白底/关闭浅灰两类行区分明显；控制台无报错）；`npm run build` 通过

  28.针对bug反馈部分，新建issue左边新增一个订阅按钮，一旦有人新建issue就会给订阅人发送消息提示，针对每一个新增的Issue，订阅的账号都能收到所有的issue信息。

- ✅ 已完成
- 实现内容：
  - **全局订阅模型**：新增 `issue_watchers` 表（user_id 唯一）；订阅后收到**所有新建 issue** 的消息提示（区别于需求26的按单 issue 订阅）
  - **接口**：`GET /api/v1/issues/watching`（查询当前用户订阅状态）、`POST /api/v1/issues/watch`（订阅，幂等）、`DELETE /api/v1/issues/watch`（取消订阅）
  - **新建 issue 通知**：创建 issue 时向所有全局订阅人（排除创建者自己）发送 `issue_new` 通知（标题「【新 Issue #x】标题」，内容「xx 提交了新的问题反馈」），关联 `issue_id` 可跳转；`NotifyIssue` 扩展为可传通知类型（issue_comment / issue_new）
  - **前端**：Bug 反馈页「新建 Issue」按钮**左侧**新增「🔔 订阅 / 🔕 已订阅」切换按钮（进入页面时加载状态）；issue_new 通知复用需求24右上角弹窗机制，点击跳转 `/issues/:id` 问题详情；通知中心正常展示
- 验证：接口测试（admin 全局订阅 watching=true、wang 创建 issue 后 admin 收到 issue_new 通知含 issue_id、取消订阅后不再收到、重复订阅幂等、自己创建不通知自己）；浏览器实测（订阅按钮位于新建 issue 左侧且初始灰色、点击切换为琥珀色「已订阅」、wang 创建 issue 后右上角弹出「【新 Issue #16】全局订阅弹窗测试…」并点击跳转 issue 详情、通知中心未读数递增且展示 issue_new 通知、再次点击取消订阅恢复灰色、控制台无报错）；`go build` / `npm run build` 通过

  29.针对别人发过来的指令需要进行动态刷新，而不是手动刷新页面

- ✅ 已完成
- 实现内容（协同作战房间「领导下发指令」功能从骨架补全为可用）：
  - **后端**：新增 `collaboration_commands` 表与 `CollaborationCommand` 模型；`POST /api/v1/rooms/:note_id/command` 真实实现——校验内容（≤2000 字）、记录下发人（真实姓名）、**持久化**指令、通过 hub 新增的 `BroadcastToRoom` 向房间内所有在线客户端**实时广播** `command:broadcast`；新增 `GET /api/v1/rooms/:note_id/commands` 指令历史接口（时间正序，默认 50 条）；下发权限沿用原角色限制（super_admin/dept_admin/group_leader/company_leader），成员可查看
  - **前端**：
    - 修复 WS 连接：协同房间原先用 socket.io-client 连 `/ws/notes/:id`，而后端是 gorilla 原生 WebSocket（`/ws/:note_id?token=`、JSON event 协议），握手永远失败 → 改为原生 WebSocket，连接状态正常显示「已连接」绿点
    - `CollaborationPage.vue`：接入「领导下发指令」输入框（回车/按钮发送）+「📋 指令记录」实时列表（进入加载历史、新指令自动追加、自动滚动到底部、实时显示下发人/时间/内容）
    - `collaboration` store：`sendCommand` 改为 REST 发送（持久化+广播）、`fetchCommands` 加载历史、监听 `command:broadcast` 去重后动态 push
- 验证：接口测试（admin/wang 下发 200 且记录真实姓名、wang 查询历史、zhang 普通用户下发 403 但可查看、指令按时间正序返回）；浏览器实测（右上角「已连接」绿点、历史指令加载、自己下发即时上屏且输入框清空、wang 通过接口发指令后**不刷新页面**约 2 秒内自动出现「🎯 王大队：…」且网络无新页面加载、控制台无应用报错）；`go build` / `npm run build` 通过

  30.针对别人发过来的指派任务需要对工作台进行动态刷新，而不是自己刷新页面

- ✅ 已完成
- 实现内容：
  - **后端**：`NotificationService` 新增 `PushNoteUpdate(userID, noteID, action)`——向用户个人 WS 通道推送 `{"event":"note:updated","note_id","action"}` 事件；`NoteService.Create` 创建指派/抄送任务时，向被指派人/抄送人（排除创建者自己）推送 `note:updated(action=created)`，使工作台实时刷新
  - **前端**：notification store 的 WS 新增 `note:updated` 事件处理（更新 `lastNoteUpdate` 并自增 `noteUpdateTick`）；`WorkbenchPage` watch `noteUpdateTick` 自动调用 `fetchNotes()`（沿用当前筛选），新指派任务卡片**无需手动刷新**即自动出现
  - **可靠性修复**：ws Hub 的 `broadcast` channel 由无缓冲改为**带缓冲（256）**，避免通知+任务推送背靠背发送时非阻塞发送被静默丢弃
- 关键 Bug 定位与修复：初测不生效——排查发现 GORM `Create` 在插入 has-many 子记录后会**清空内存中的 `note.Assignees` 切片**（推送循环遍历为空），改用原始请求 `req.Assignees` 后推送正常（WS 探针实测 `note:updated` 事件送达）
- 验证：WS 探针（wang 指派任务给 admin → admin 通道实时收到 `note:updated(action=created)` 事件）；浏览器实测（wang 通过接口指派任务后，admin 工作台**不刷新页面**约 2 秒内新卡片自动出现在列表首位、`noteUpdateTick` 0→1、自动发起 `GET /api/v1/notes` 且 HTTP 200、未读角标同步 +1、控制台无应用报错）；`go build` / `npm run build` 通过

  31.无音频设备下的任务/消息提醒：页面标题闪烁 + favicon 未读红点 + 系统级通知（Notification API）

- ✅ 已完成（2026-08-27，咨询后落地，纯前端改动 notification store）
- 实现内容：
  - **标题闪烁**：watch 未读数，>0 时每 1.2s 在「【N条新消息】轻燕工作台」与「轻燕工作台」间交替；清零恢复原标题
  - **favicon 未读红点**：canvas 动态绘制带红色角标（N 或 99+）的图标（data URL），未读 >0 时替换 `link[rel=icon]`；清零恢复默认 `/favicon.svg`（无需额外资源文件）
  - **系统级通知**：收到任务通知/聊天消息且**页面在后台**（`document.hidden`）时，通过 Notification API 弹系统通知（标题+内容+图标，不依赖音频）；点击系统通知聚焦窗口并跳转对应页面（任务详情 `/workbench?note=`、聊天 `/chat?peer=`、issue 详情 `/issues/:id`、通知中心）；首次收到消息时请求通知权限（避免一进页面就弹授权框）；页面可见时仍用右上角弹窗（需求24）
- 说明：`navigator.vibrate()`（网页震动）仅 Android Chrome 支持、桌面 PC 无效；浏览器无法真正将网页窗口置顶（全屏需用户手势且不可自动持续）——本方案为浏览器端最有效的无声提醒组合
- 验证：浏览器实测（未读 27 时标题交替闪烁、favicon 为 data URL 红点；点击全部已读后 title 恢复、favicon 恢复 /favicon.svg；再次指派任务后角标 0→1 且 favicon/标题联动恢复闪烁；`Notification.permission=granted`，手动构造系统通知成功捕获（页面可见时按设计不构造）；控制台无应用报错）；`npm run build` 通过

  32.鼠标移动到表情包图片上时放大图片，方便筛选查看以免发错内容

- ✅ 已完成（2026-08-27，纯前端改动 EmojiPicker.vue）
- 实现内容：
  - 图片表情网格每项增加 `@mouseenter/@mouseleave`：悬停时记录当前表情与鼠标坐标，移出时清空
  - 新增**放大预览浮层**：`Teleport to="body"` 固定定位（`fixed z-[200] pointer-events-none`），显示 112x112 大图（`object-contain`）+ 表情名称，白底圆角卡片 + 阴影 + 边框；跟随鼠标位置，靠近视口边缘时自动反向偏移避免溢出；`pointer-events-none` 保证不遮挡点击发送
- 验证：浏览器实测（聊天页打开表情选择器 → 切到「熊猫头表情包」→ 悬停 001.jpg 出现 112x112 大图+名称预览、移动切换到 002.jpg、派发 mouseleave 后浮层移除、点击表情正常发送图片消息不回归、控制台无应用报错）；`npm run build` 通过

  33.点开表情包后点击页面其他地方也能关闭表情选择弹窗

- ✅ 已完成（2026-08-27，纯前端改动 ChatPage.vue）
- 实现内容：给 😊 表情按钮与表情面板容器绑定 `ref`；`onMounted` 时在 `document` 上注册 `mousedown` 监听——点击目标不在表情面板与触发按钮内时关闭面板（点击面板内部/按钮本身不误关），`onUnmounted` 移除监听；与原有按钮 toggle、输入框 focus 关闭逻辑兼容
- 验证：浏览器实测（点 😊 打开面板 → 点击左侧会话列表等外部区域面板关闭；再打开点击面板内 emoji 😂 正常插入且面板保持打开不误关；点 😊 按钮 toggle 正常关闭/再开；控制台无报错）；`npm run build` 通过

  34.在任务创建和编辑这里可以全屏编写任务内容，并且需要支持markdown的语法，同样的模板库管理这个板块的的模板内容部分也要支持markdown格式的语法

- ✅ 已完成（2026-08-28，前端改动）
- 实现内容：
  - 新增依赖：`marked`（Markdown → HTML）、`turndown`（HTML → Markdown）
  - 新增工具 [markdown.ts](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/utils/markdown.ts)：`markdownToHtml`（GFM+单换行转 `<br>`，保存时转 HTML 与既有富文本展示兼容）；`htmlToMarkdown`（打开旧富文本任务时转回 Markdown 源码，纯文本原样返回避免转义 markdown 符号）
  - 新增组件 [MarkdownEditor.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/components/common/MarkdownEditor.vue)：Markdown 源码编辑 + 渲染预览双标签页；工具栏（H1/H2/H3、加粗/斜体/删除线、无序/有序列表、引用、行内代码/代码块、链接、图片、表格）；右上角**全屏编辑**按钮（fixed 覆盖全屏，Esc 退出），预览用 `prose` 排版
  - 任务创建/编辑（[WorkbenchPage.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/pages/WorkbenchPage.vue)）：创建弹窗与详情编辑弹窗的内容编辑器由富文本替换为 MarkdownEditor，保存时 markdown→HTML，编辑旧任务时 HTML→markdown 回填；「使用模板」改为以 Markdown（`> 📋 模板：名称` 引用头）直接填入
  - 模板库管理（[TemplatesPage.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/pages/admin/TemplatesPage.vue)）：模板内容由 textarea 替换为 MarkdownEditor（编辑/预览/全屏）；卡片内容预览改为 Markdown 渲染（紧凑 prose）
- 验证：浏览器实测（admin 登录 → 工作台「新建任务」弹窗：Markdown 编辑器工具栏/编辑/预览齐全，输入 `# 标题`、`- 列表项` 预览渲染正确；点全屏按钮编辑器覆盖全屏、可退出；选「数据分析研判模板」后 markdown 模板内容自动填入；/admin/templates 添加/编辑模板弹窗均为 Markdown 编辑器，通用任务模板内容正确回填，预览/全屏正常；控制台无编辑器相关报错）；`npm run build` 通过

  35.归档查询这里点击归档的记录，点击后不是通过右侧进行详情展示，而是通过新页面进行展示，而且新页面能看到任务反馈的内容。

- ✅ 已完成（2026-08-28，前端改动）
- 实现内容：
  - 新增路由 `/workbench/archive/:id` → `ArchiveDetail`（[router/index.ts](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/router/index.ts)）
  - 新增 [ArchiveDetailPage.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/pages/ArchiveDetailPage.vue)：通过 `fetchNoteById` 加载单条归档任务；顶部「← 返回归档列表」按钮；展示标题/已归档/来源类型标签、创建人信息条、任务内容（Markdown 渲染）、标签；📋任务信息卡片（来源类型/创建/完成/归档时间/模板类型/被指派人/抄送人）；**💬任务反馈卡片**——遍历 `assignees.feedback_content` 渲染绿色反馈气泡（含反馈人/时间/内容/完成时间），带「补充反馈」入口（FeedbackModal，提交后刷新列表）；底部「恢复任务至工作台」「返回列表」按钮
  - 改造 [ArchivePage.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/pages/ArchivePage.vue)：`openDetail` 改为 `router.push('/workbench/archive/:id')` 跳转新页面；移除右侧侧滑面板（`slide-panel`）及 `showDetailPanel`/`selectedNote`/`closeDetail` 等相关状态与死代码（FeedbackModal/useToast/submitFeedback）
- 验证：浏览器实测（admin 登录 → /workbench/archive 共 14 条归档 → 点击「反馈同步验证任务」→ 跳转新页面 /workbench/archive/:id 而非侧滑面板；页面含返回按钮、标题、来源/创建/完成/归档时间、被指派人张三、**💬任务反馈（1）：张三「报表已汇总，共 50 份」**、补充反馈/恢复/返回按钮；点「← 返回归档列表」回到列表页；控制台无报错）；`npm run build` 通过

  36.现在需要新增一个功能就是针对所有的搜索框支持拼音搜索以及拼音搜字母搜索（无视大小写）

- ✅ 已完成（2026-08-31，前后端改动）
- 实现内容：
  - **前端（4 处本地过滤搜索）**：新增 [pinyin.ts](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/utils/pinyin.ts) 工具——`matchPinyin(keyword, ...texts)` 统一入口（基于 pinyin-pro 的 `match()`，内部自动忽略大小写，支持全拼/首字母/混合输入，并保留原文直接命中），引入依赖 `pinyin-pro`
    - [UserPicker.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/components/common/UserPicker.vue)：人员选择器按姓名/部门拼音过滤
    - [ChatPage.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/pages/ChatPage.vue)：聊天通讯录按姓名/用户名/部门拼音过滤
    - [InspectWorkbenchPage.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/pages/InspectWorkbenchPage.vue)：用户工作台人员搜索拼音过滤
    - [TagSelector.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/components/common/TagSelector.vue)：标签选择器按标签名/子标签拼音过滤（不影响搜索框兼作新建标签名称输入的原有功能）
  - **后端（6 处服务端过滤搜索）**：新增 [pinyin.go](file:///Users/admin/codespace/golang/Zephyr/Server-code/internal/utils/pinyin.go)（`IsPinyinKeyword` 判定纯 ASCII 关键词、`pinyinVariants` 生成全拼/首字母变体、`MatchKeyword` 忽略大小写匹配，基于 go-pinyin，sync.Once 初始化 + RWMutex 拼音缓存）与 [pinyin_support.go](file:///Users/admin/codespace/golang/Zephyr/Server-code/internal/repository/pinyin_support.go)（泛型 `pageSlice` 内存分页辅助）
    - 策略：关键词为纯 ASCII（拼音/字母）时走「全量加载 → 内存拼音过滤（全拼/首字母，大小写不敏感）→ 手动分页」，中文关键词保持原 SQL ILIKE；note_repo 由 LIKE 统一为 ILIKE
    - 覆盖接口：任务列表 notes（[note_repo.go](file:///Users/admin/codespace/golang/Zephyr/Server-code/internal/repository/note_repo.go)，标题/内容）、Bug 反馈 issues（[issue_repo.go](file:///Users/admin/codespace/golang/Zephyr/Server-code/internal/repository/issue_repo.go)，标题/内容）、模板 templates 与专项工作组 groups（[business_repo.go](file:///Users/admin/codespace/golang/Zephyr/Server-code/internal/repository/business_repo.go)，名称/简介/内容、名称/简介）、操作日志 operation-logs 与工作报告 work-reports（[system_repo.go](file:///Users/admin/codespace/golang/Zephyr/Server-code/internal/repository/system_repo.go)，用户名、标题/内容/用户名）
- 验证：
  - 后端单测：`go test ./internal/utils -run TestMatchKeyword -v` 12 个表驱动用例全部 PASS（全拼/首字母/混合/大小写/中文回退 SQL）
  - 构建：后端 `go build` 通过；前端 `npm run build` 通过（pinyin-pro 171.27 kB 打包产物）
  - 浏览器实测 12 项场景全部通过——前端：聊天通讯录「zs」命中周干事+张三、「lty」命中刘探员、「WuZhu」混合大小写全拼命中吴主任；用户工作台「lty」「LIUTAN」均命中刘探员；新建任务弹窗人员选择「lty」仅命中刘探员；标签选择「jj」仅命中「紧急」、「QingBao」全拼命中「情报研判」。后端：归档查询「djs」14→3 条命中倒计时任务、「LEITING」大写全拼命中雷霆行动任务；模板管理「XieCha」命中紧急协查通报模板；专项工作组「qfxz」命中清风小组（余下 issues/操作日志/工作报告三接口与已测接口共用同一 MatchKeyword 代码路径）

    37.在个人中心处支持上传头像以及上传平台背景图进行更换，并且可以调整背景图的透明度以及填充方式。 ✅

- 实现：
  - **后端**：
    - [user.go](file:///Users/admin/codespace/golang/Zephyr/Server-code/internal/models/user.go)：User 模型新增 `background`（背景图地址）、`bg_opacity`（透明度 0~1，默认 1）、`bg_fill`（填充方式 cover/contain/fill/tile，默认 cover），AutoMigrate 自动建列
    - [upload_handler.go](file:///Users/admin/codespace/golang/Zephyr/Server-code/internal/handlers/upload_handler.go)：新增 `POST /api/v1/uploads/image` 通用图片上传（仅 png/jpg/gif/webp，魔数校验防伪（RIFF 容器进一步确认 WEBP 标识），≤10MB，落盘 `uploads/images/`）
    - [user_service.go](file:///Users/admin/codespace/golang/Zephyr/Server-code/internal/services/user_service.go) + [user_handler.go](file:///Users/admin/codespace/golang/Zephyr/Server-code/internal/handlers/user_handler.go)：新增 `PUT /api/v1/profile` 自助更新接口（仅本人，普通用户可用），支持 name/rank/phone/email/avatar/background/bg_opacity/bg_fill，透明度钳制 [0,1]、填充方式白名单校验
  - **前端**：
    - [ProfilePage.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/pages/ProfilePage.vue)：用户卡头像支持点击更换（悬停遮罩提示「更换头像」，上传成功即时生效并同步 localStorage）；新增「平台背景」卡片——预览区实时反映调整、上传/更换/移除背景图、透明度滑块（0~100%，5% 步进）、填充方式四选一（适应裁剪/完整显示/拉伸填充/平铺）；个人信息表单保存切换为自助接口（修复普通用户 403）
    - [useUserBackground.ts](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/composables/useUserBackground.ts) + [AdminLayout.vue](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/layouts/AdminLayout.vue)：全局背景层（fixed + z-0，内容区 Sidebar/TopBar 保持不透明层级 z-10），卡片不透明保证可读性，保存后全平台即时生效
    - [admin.ts](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/services/admin.ts)：新增 `updateMyProfile` / `uploadImage` 接口封装；[user.ts](file:///Users/admin/codespace/golang/Zephyr/Web-Front/src/types/user.ts)：User 类型补充 background/bg_opacity/bg_fill
- 验证：
  - 后端 `go build ./...` 通过；前端 `npm run build`（vue-tsc + vite）通过 1.53s
