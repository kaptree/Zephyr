# 轻燕工作台 —— 服务端开发文档

> 版本：v2.0  
> 定位：面向公司系统的轻量化智能协同办公支撑解决方案 · RESTful API 服务端  
> 核心职责：任务全生命周期管理、专项工作组一键分发、权限控制、实时协同同步、归档追溯、工作成效分析

---

## 一、项目概述

服务端采用 `Go + Gin + GORM` 技术栈，提供高并发、低延迟的 RESTful API 服务，支撑 Web 前端（Vue 3）的多端数据一致性与实时协同。数据库使用 PostgreSQL，利用 JSONB 类型存储协同画布内容。架构采用经典三层分层：`Handler → Service → Repository`。

---

## 二、技术栈与中间件

| 层级        | 技术选型                    | 说明                                    |
| ----------- | --------------------------- | --------------------------------------- |
| 语言 / 框架 | Go 1.22 + Gin               | 高性能 HTTP 框架                        |
| ORM         | GORM v2 + PostgreSQL Driver | 模型映射、AutoMigrate、关联查询         |
| 数据库      | PostgreSQL 15+              | 主数据库，JSONB / 数组 / ILIKE 全文检索 |
| 缓存        | Redis                       | 会话、热点数据                          |
| WebSocket   | gorilla/websocket           | 实时协同通信                            |
| 认证        | JWT RS256                   | Access Token + Refresh Token 双令牌     |
| 权限        | 自研 RBAC                   | 角色 + 数据权限 Scope 隔离              |
| 日志        | Zap + Lumberjack            | 结构化日志 + 文件轮转                   |
| UUID        | google/uuid                 | 全局唯一标识                            |

---

## 三、项目目录结构

```
Server-code/
├── main.go                      # 入口：Config → DB → Redis → JWT → Router → HTTP Server
├── go.mod / go.sum              # Go 模块依赖
├── config.json                  # 主配置文件（Server / Database / Redis / JWT / WS / RateLimit）
├── Makefile                     # 构建/运行/测试脚本
├── Dockerfile / docker-compose.yml
├── cmd/
│   └── seed/
│       └── main.go              # 种子数据：用户、部门、标签、模板
├── internal/
│   ├── config/
│   │   └── config.go            # 配置结构体 + 文件加载 + 环境变量覆盖
│   ├── database/
│   │   ├── postgres.go          # PostgreSQL GORM 连接初始化
│   │   └── redis.go             # Redis 连接初始化
│   ├── handlers/
│   │   ├── auth_handler.go      # 登录/登出/注册/刷新令牌/当前用户
│   │   ├── user_handler.go      # 用户 CRUD + 可见用户列表
│   │   ├── dept_handler.go      # 部门树 CRUD
│   │   ├── note_handler.go      # 任务 CRUD + 完成/盯办/删除/恢复/统计/导出
│   │   ├── tag_handler.go       # 标签 CRUD
│   │   ├── business_handler.go  # 模板列表/工作组管理/协同房间/台账
│   │   ├── analytics_handler.go # 个人统计/智能报告/报告模板编辑
│   │   └── system_handler.go    # 系统配置/AI配置/配置文件/操作日志
│   ├── logger/
│   │   ├── logger.go            # Zap 日志初始化（开发/生产模式）
│   │   └── writer.go            # Lumberjack 日志轮转
│   ├── middleware/
│   │   ├── auth.go              # JWT 验证 + RequireRoles 权限中间件
│   │   ├── cors.go              # CORS 跨域配置
│   │   ├── recovery.go          # Panic 恢复
│   │   ├── ratelimit.go         # 令牌桶限流
│   │   ├── request_logger.go    # 请求日志记录
│   │   └── operation_logger.go  # 操作日志自动记录（POST/PUT/DELETE）
│   ├── models/
│   │   ├── note.go              # Note / NoteAssignee / NoteAttachment
│   │   ├── user.go              # User / Department / RolePermission
│   │   ├── business.go          # Tag / Template / WorkGroup / WorkGroupMember / CollaborationRoom / Reminder / LedgerEntry
│   │   └── system.go            # SystemConfig / AIConfig / ConfigFileHistory / AdminLog / OperationLog / WorkReport / ReportTemplate
│   ├── repository/
│   │   ├── note_repo.go         # Note CRUD + 序列号 + 统计 + 个人统计 + ListByGroup
│   │   ├── user_repo.go         # User CRUD + 权限查询
│   │   ├── dept_repo.go         # Department 树查询 + CRUD
│   │   ├── business_repo.go     # Tag / Template / WorkGroup（Search/SearchFilter）/ Room / Ledger
│   │   └── system_repo.go       # 系统配置 / AI配置 / 日志 / 报告模板
│   ├── router/
│   │   └── router.go            # ★ 全部路由注册 + 中间件链
│   ├── services/
│   │   ├── auth_service.go      # 认证业务逻辑
│   │   ├── note_service.go      # 任务业务逻辑
│   │   └── user_service.go      # 用户业务逻辑
│   ├── utils/
│   │   ├── crypto.go            # 随机数生成
│   │   ├── jwt.go               # JWT RS256 生成/解析
│   │   ├── password.go          # bcrypt 密码哈希
│   │   └── response.go          # 统一响应：Success / Created / Paginated / BadRequest / NotFound / InternalError
│   └── ws/
│       └── hub.go               # WebSocket Hub（Room 模式）
├── pkg/
│   └── errors/
│       └── errors.go            # 自定义错误类型
└── tasks/                        # 开发任务跟踪文档
    ├── 01-项目脚手架与基础设施.md
    ├── 02-用户认证与权限系统.md
    ├── 03-任务管理核心.md
    ├── 04-标签与模板管理.md
    ├── 05-协同与工作组.md
    ├── 06-盯办提醒与台账系统.md
    └── 07-安全加固与系统优化.md
```

---

## 四、数据库设计（核心表结构）

### 4.1 用户与组织

**`users`**

| 字段              | 类型                  | 说明                                             |
| ----------------- | --------------------- | ------------------------------------------------ |
| `id`              | UUID PK               |                                                  |
| `username`        | varchar UNIQUE        | 登录账号                                         |
| `name`            | varchar               | 姓名                                             |
| `password_hash`   | varchar               | bcrypt 加密                                      |
| `department_id`   | UUID FK → departments | 所属部门                                         |
| `role`            | varchar               | super_admin / dept_admin / group_leader / member |
| `rank`            | varchar               | 职级                                             |
| `position`        | varchar               | 岗位（如：刑侦民警、数据分析师）                 |
| `skills`          | text                  | 技能特长（JSON数组或逗号分隔文本）               |
| `phone` / `email` | varchar               | 联系方式                                         |
| `avatar`          | varchar               | 头像 URL                                         |
| `is_active`       | bool                  | 是否启用                                         |
| `last_login_at`   | timestamp             | 最后登录时间                                     |

**`departments`**

| 字段         | 类型              | 说明       |
| ------------ | ----------------- | ---------- |
| `id`         | UUID PK           |            |
| `name`       | varchar           | 部门名称   |
| `parent_id`  | UUID FK（自关联） | 树形结构   |
| `level`      | int               | 层级       |
| `sort_order` | int               | 排序       |
| `leader_id`  | UUID FK → users   | 部门负责人 |

### 4.2 任务核心

**`notes`**

| 字段                | 类型                      | 说明                                                                                 |
| ------------------- | ------------------------- | ------------------------------------------------------------------------------------ |
| `id`                | UUID PK                   |                                                                                      |
| `title`             | varchar(200)              | 标题                                                                                 |
| `content`           | text                      | 正文（纯文本/Markdown）                                                              |
| `color_status`      | varchar                   | yellow / red / green                                                                 |
| `source_type`       | varchar                   | self / assigned / collaboration                                                      |
| `template_type`     | varchar                   | default / data_analysis / special_project / emergency_canvas / collaborative_writing |
| `serial_no`         | varchar                   | 自动生成流水号                                                                       |
| `creator_id`        | UUID FK → users           | 创建人                                                                               |
| `owner_id`          | UUID FK → users           | 负责人                                                                               |
| `assigner_id`       | UUID FK → users           | 指派人                                                                               |
| `department_id`     | UUID FK → departments     | 所属部门                                                                             |
| `group_id`          | UUID FK → work_groups     | ★ 专项工作组 ID                                                                      |
| `due_time`          | timestamp                 | 截止时间（工作时间自动计算）                                                         |
| `work_time_seconds` | int                       | ★ 工作时间（秒）：指派时选择，任务下发即开始倒计时，据此自动生成 due_time            |
| `due_remind_at`     | timestamp                 | ★ 到期提醒已发送时间（去重，避免重复发送）                                           |
| `completed_at`      | timestamp                 | 完成时间                                                                             |
| `remind_count`      | int                       | 盯办次数                                                                             |
| `is_archived`       | bool                      | 是否归档                                                                             |
| `archive_time`      | timestamp                 | 归档时间                                                                             |
| `tags`              | many2many → tags          | 标签关联                                                                             |
| `assignees`         | has_many → note_assignees | 指派/协同人员                                                                        |

**`note_assignees`**

| 字段               | 类型      | 说明                                        |
| ------------------ | --------- | ------------------------------------------- |
| `note_id`          | UUID PK   |                                             |
| `user_id`          | UUID PK   |                                             |
| `role_in_note`     | varchar   | member / leader / initiator（发起者）       |
| `sign_status`      | varchar   | ★ 签收状态：unsigned 未签收 / signed 已签收 |
| `signed_at`        | timestamp | ★ 签收时间                                  |
| `feedback_content` | text      | 反馈内容                                    |
| `feedback_at`      | timestamp | 反馈时间                                    |
| `is_read`          | bool      | 是否已读                                    |

**`note_ccs`**（★ 需求20 抄送人）

| 字段         | 类型      | 说明     |
| ------------ | --------- | -------- |
| `note_id`    | UUID PK   |          |
| `user_id`    | UUID PK   |          |
| `created_at` | timestamp | 抄送时间 |

> 抄送人可查看任务（紫色卡片 +「抄送」徽章 +「任务抄送」来源标签），并拥有「完成并归档 / 删除」能力；编辑/盯办/签收仍仅限参与者（`CanManage` 排除抄送人）

### 4.3 标签与模板

**`tags`** — 字段：`id / name / color / category / scope / sort_order`
**`templates`** — 字段：`id / name / type / fields(JSONB) / layout / is_system`

### 4.4 专项工作组

**`work_groups`**

| 字段            | 类型            | 说明               |
| --------------- | --------------- | ------------------ |
| `id`            | UUID PK         |                    |
| `name`          | varchar(200)    | 工作组名称         |
| `description`   | text            | ★ 工作要求描述     |
| `initiator_id`  | UUID FK → users | 发起人             |
| `template_type` | varchar         | ★ 模板类型         |
| `status`        | varchar         | active / completed |
| `due_time`      | timestamp       | ★ 截止时间         |
| `note_id`       | UUID FK         | 关联任务           |

**`work_group_members`** — 字段：`group_id PK / user_id PK / role(leader/member) / sub_group_name`

### 4.5 台账与日志

**`ledger_entries`** — 字段：`id / note_id / user_id / action / action_detail / ip_address / user_agent / created_at`
**`reminders`** — 字段：`id / note_id / reminder_id / target_id / message / is_acknowledged`
**`operation_logs`** — 字段：`id / user_id / user_name / action / method / path / status_code / ip_address`
**`admin_logs`** — 字段：`id / user_id / action / resource / resource_id / detail(JSONB)`

### 4.6 系统配置与报告

**`system_configs`** — 系统级 KV 配置
**`ai_configs`** — AI 提供商配置（Provider / Model / APIKey / Endpoint）
**`work_reports`** — 工作成效报告（UserID / Period / Content(JSONB) / **Category**：personal 个人 / team 团队 / group 工作组，团队报告与个人报告在报告历史中用 tag 区分）
**`report_templates`** — 报告模板（ID PK / name / content / creator_id）

### 4.7 通知与聊天（★ 新增）

**`notifications`** — 系统通知表

| 字段           | 类型      | 说明                                                                                       |
| -------------- | --------- | ------------------------------------------------------------------------------------------ |
| `id`           | uuid PK   | 主键（gen_random_uuid）                                                                    |
| `recipient_id` | uuid      | 接收人（index）                                                                            |
| `sender_id`    | uuid      | 发起人（可空，关联 User）                                                                  |
| `note_id`      | uuid      | 关联任务（可空，index）                                                                    |
| `type`         | varchar   | 事件类型：task_assigned / task_completed / task_feedback / task_remind / task_due / system |
| `title`        | varchar   | 标题                                                                                       |
| `content`      | text      | 内容（富文本/摘要）                                                                        |
| `is_read`      | bool      | 是否已读（index）                                                                          |
| `is_deleted`   | bool      | 是否删除（软删除）                                                                         |
| `read_at`      | timestamp | 已读时间                                                                                   |
| `created_at`   | timestamp | 创建时间                                                                                   |

**`chat_messages`** — 用户一对一聊天表

| 字段          | 类型      | 说明                    |
| ------------- | --------- | ----------------------- |
| `id`          | uuid PK   | 主键（gen_random_uuid） |
| `sender_id`   | uuid      | 发送人（index）         |
| `receiver_id` | uuid      | 接收人（index）         |
| `note_id`     | uuid      | 关联任务（可空）        |
| `content`     | text      | 消息内容（富文本）      |
| `is_read`     | bool      | 是否已读（index）       |
| `read_at`     | timestamp | 已读时间                |
| `created_at`  | timestamp | 创建时间                |

### 4.8 到期提醒调度器（★ 新增）

- 后端启动时若 `scheduler.auto_remind_enabled = true`，则启动定时调度器（间隔 `auto_remind_interval_minutes`，默认 10 分钟）
- 每次扫描：`due_time <= now + 2h` 且 `completed_at IS NULL` 且 `is_archived = false` 且 `due_remind_at IS NULL` 的未完成任务
- 命中后向「负责人 + 发起人 + 全部被指派人」发送 `task_due` 到期提醒通知（WebSocket 实时推送），并写入 `due_remind_at` 去重，同一任务仅提醒一次
- 任务完成归档后不再参与扫描（completed_at 非空）；前端任务便签左上角倒计时在完成/归档后自动隐藏

---

## 五、完整 API 路由表

### 5.1 全局约定

- **Base Path**：`/api/v1`
- **认证头**：`Authorization: Bearer <token>`
- **响应格式**：`{ code, message, data, timestamp }`
- **分页**：`page`（默认1）、`page_size`（默认20，最大100）

### 5.2 认证接口

| 方法 | 路径            | Handler        | 说明                   |
| ---- | --------------- | -------------- | ---------------------- |
| POST | `/auth/login`   | Login          | 登录，返回 Token       |
| POST | `/auth/refresh` | RefreshToken   | 刷新令牌               |
| POST | `/auth/logout`  | Logout         | 登出（需认证）         |
| GET  | `/auth/me`      | GetCurrentUser | 当前用户信息（需认证） |

### 5.3 用户管理

| 方法   | 路径                       | Handler            | 说明                       |
| ------ | -------------------------- | ------------------ | -------------------------- |
| POST   | `/users`                   | Register           | 创建用户（admin+）         |
| GET    | `/users`                   | ListUsers          | 用户列表（分页）           |
| GET    | `/users/visible`           | GetVisibleUsers    | 可见用户（选人器用）       |
| GET    | `/users/with-stats`        | ListUsersWithStats | 用户列表（含工作类型统计） |
| GET    | `/users/recommend`         | RecommendUsers     | 按工作类型智能推荐人员     |
| GET    | `/users/work-type-options` | WorkTypeOptions    | 工作类型选项列表           |
| GET    | `/users/:id`               | GetUser            | 用户详情                   |
| GET    | `/users/:id/profile`       | GetUserProfile     | 用户完整档案（含参与统计） |
| PUT    | `/users/:id`               | UpdateUser         | 更新用户（含岗位/技能）    |
| DELETE | `/users/:id`               | DeleteUser         | 删除用户                   |

### 5.4 预设组管理

| 方法   | 路径           | Handler | 说明                             |
| ------ | -------------- | ------- | -------------------------------- |
| GET    | `/presets`     | List    | 预设组列表（可选按工作类型筛选） |
| GET    | `/presets/:id` | Get     | 预设组详情                       |
| POST   | `/presets`     | Create  | 创建预设组（含成员）             |
| PUT    | `/presets/:id` | Update  | 更新预设组（含成员）             |
| DELETE | `/presets/:id` | Delete  | 删除预设组                       |

#### 5.4.1 数据模型

**`preset_groups`**

| 字段            | 类型            | 说明         |
| --------------- | --------------- | ------------ |
| `id`            | UUID PK         |              |
| `name`          | varchar         | 预设组名称   |
| `description`   | text            | 描述         |
| `template_type` | varchar         | 适用工作类型 |
| `creator_id`    | UUID FK → users | 创建者       |
| `created_at`    | timestamp       |              |
| `updated_at`    | timestamp       |              |

**`preset_group_members`**

| 字段             | 类型                       | 说明 |
| ---------------- | -------------------------- | ---- |
| `preset_id`      | UUID PK FK → preset_groups |      |
| `user_id`        | UUID PK FK → users         |      |
| `role`           | varchar                    | 角色 |
| `sub_group_name` | varchar                    | 子组 |

### 5.5 部门管理

| 方法   | 路径               | Handler   | 说明                     |
| ------ | ------------------ | --------- | ------------------------ |
| GET    | `/departments`     | GetTree   | 部门树（支持 flat 参数） |
| GET    | `/departments/:id` | GetDetail | 部门详情                 |
| POST   | `/departments`     | Create    | 创建部门                 |
| PUT    | `/departments/:id` | Update    | 更新部门                 |
| DELETE | `/departments/:id` | Delete    | 删除部门                 |

### 5.5 任务管理（核心）

| 方法   | 路径                                   | Handler          | 说明                                                                              |
| ------ | -------------------------------------- | ---------------- | --------------------------------------------------------------------------------- |
| GET    | `/notes`                               | ListNotes        | 任务列表（权限范围过滤，预加载 assignees 含反馈）                                 |
| POST   | `/notes`                               | CreateNote       | 创建任务（自动生成序列号+建立关联，支持 cc_user_ids 抄送）                        |
| GET    | `/notes/export`                        | ExportNotes      | 批量导出任务为 HTML                                                               |
| GET    | `/notes/:id`                           | GetNote          | 任务详情                                                                          |
| PUT    | `/notes/:id`                           | UpdateNote       | 更新任务                                                                          |
| POST   | `/notes/:id/complete`                  | CompleteNote     | 完成归档（可携带反馈 feedback_content，自动通知发起人）                           |
| POST   | `/notes/:id/feedback`                  | Feedback         | ★ 任务反馈填报（补充提交，通知发起人）                                            |
| POST   | `/notes/:id/remind`                    | RemindNote       | 盯办（color_status → red，自动通知被盯办人）                                      |
| POST   | `/notes/:id/sign`                      | SignNote         | ★ 任务签收（仅指派任务被指派人可签，幂等，通知发起人）                            |
| DELETE | `/notes/:id`                           | DeleteNote       | 软删除                                                                            |
| POST   | `/notes/:id/restore`                   | RestoreNote      | 恢复                                                                              |
| GET    | `/notes/:id/export`                    | ExportNote       | 导出单个任务为 HTML                                                               |
| GET    | `/notes/:id/attachments`               | ListAttachments  | 列出任务附件                                                                      |
| POST   | `/notes/:id/attachments`               | Upload           | 上传附件（图片/文档，最大10MB）                                                   |
| DELETE | `/notes/:id/attachments/:attachmentId` | DeleteAttachment | 删除附件                                                                          |
| GET    | `/notes/users/:userId/workbench`       | InspectUserNotes | ★ 用户工作台（公司领导/超管查看指定用户的任务，status=active/completed/archived） |
| GET    | `/notes/stats`                         | Stats            | 统计                                                                              |
| GET    | `/notes/heatmap`                       | Heatmap          | 归档活动热力图（按年查询，基于completed_at）                                      |

> **CreateNote 请求体：** `{ content, tag_ids[], source_type, collaborator_ids[], owner_id, due_time, template_id }`
> **source_type 颜色映射：** `self→yellow, assigned→red, collaboration→blue`

> **任务可见性与越权防护（★ 权限修复）：**
>
> - **列表可见范围**（`NoteRepository.List`）：`dept_admin`（部门管理员）仅本部门及子部门任务；`super_admin` / `group_leader` / 普通用户一律按「创建人 / 负责人 / 被指派人 / 抄送人」过滤——管理员不再看到所有人员的任务
> - **详情与操作越权拦截**（`CheckUserAccess`）：`GET /notes/:id`（详情）、`PUT`（编辑）、`DELETE`（删除）、`GET /notes/:id/export`（导出）、`POST /notes/:id/remind`（盯办）、`POST /notes/:id/sign`（签收，额外校验仅被指派人非发起者可签）、`POST /notes/:id/restore`（恢复）统一校验访问权（创建人/负责人/被指派人/抄送人；部门管理员含本部门及子部门），越权返回 404（不泄露任务是否存在）；**抄送人可查看/完成/删除**（卡片提供完成并归档+删除），编辑/盯办/签收仍仅限参与者（`CanManage` 排除抄送人，返回 403）
> - `CompleteNote` 仍按原权限：被指派人/发起人可完成，`super_admin`/`dept_admin` 保留办结豁免

### 5.6 标签管理（支持两级分类）

| 方法   | 路径        | Handler | 说明                                                                                                   |
| ------ | ----------- | ------- | ------------------------------------------------------------------------------------------------------ |
| GET    | `/tags`     | List    | 标签列表（按 scope/category/parent_id 过滤，支持层级预加载；返回全部标签含未使用，不再自动删除空标签） |
| POST   | `/tags`     | Create  | 创建标签（含 parent_id 建立层级关系）                                                                  |
| PUT    | `/tags/:id` | Update  | 更新标签                                                                                               |
| DELETE | `/tags/:id` | Delete  | ★ 删除标签：级联清理所有任务上的该标签关联（note_tags），一级标签连同子标签一并删除，保持标签一致性    |

> 标签内置9大一级分类：治安管控、巡逻防控、矛盾调解、线索核查、专案办理、安保勤务、舆情处置、内勤事务、其他工作，每项含5个二级细分类别。
>
> **标签修复（★ Bug）：**
>
> - 列表查询移除「使用次数为 0 自动删除 + 仅返回已使用标签」的错误逻辑，新建的一级/二级标签持久保留、正常展示，不再消失
> - 删除接口移除「标签使用中禁止删除」限制，改为事务级联：删除标签自身 → 清理 `note_tags` 关联（含子标签）→ 任务上不再残留该标签，保持标签一致性

### 5.7 模板管理（支持用户自定义）

| 方法   | 路径             | Handler | 说明                                        |
| ------ | ---------------- | ------- | ------------------------------------------- |
| GET    | `/templates`     | List    | 模板列表（按 type 过滤）                    |
| GET    | `/templates/:id` | Get     | 模板详情                                    |
| POST   | `/templates`     | Create  | ★ 创建自定义模板（name/type/fields/layout） |
| PUT    | `/templates/:id` | Update  | ★ 编辑模板（系统模板也可编辑）              |
| DELETE | `/templates/:id` | Delete  | ★ 删除模板（系统内置模板不可删除）          |

> 模板类型：`default`(通用任务)、`data_analysis`(数据分析)、`special_project`(专项行动)、`emergency_canvas`(紧急协查)、`collaborative_writing`(协同作战)、`custom`(自定义)
> 创建任务时可选择模板预填内容，系统自动将模板字段以 `📋 模板：{名称}` 格式插入

### 5.8 专项工作组（★ 核心功能）

| 方法   | 路径                           | Handler         | 说明                                                                                   |
| ------ | ------------------------------ | --------------- | -------------------------------------------------------------------------------------- |
| GET    | `/groups`                      | Search          | ★ 搜索工作组（keyword / user_id / date_from / date_to 组合筛选，ILIKE 匹配，分页）     |
| GET    | `/groups/mine`                 | MyGroups        | 我的工作组（我发起的 + 我参与的）                                                      |
| POST   | `/groups`                      | Create          | ★ 一键创建工作组（自动为每位成员创建 assigned 任务 + 生成序列号 + 建立 assignee 关联） |
| GET    | `/groups/:id`                  | GetDetail       | 工作组详情（含 members + initiator）                                                   |
| DELETE | `/groups/:id`                  | Delete          | 删除工作组（级联删除成员关联）                                                         |
| GET    | `/groups/:id/members`          | GetMembers      | 工作组成员列表                                                                         |
| PUT    | `/groups/:id/members/:user_id` | UpdateMember    | 更新成员角色/子组                                                                      |
| GET    | `/groups/:id/notes`            | GetGroupNotes   | ★ 工作组专属任务列表（仅按 group_id 过滤，独立于日常任务）                             |
| POST   | `/groups/:id/notes`            | CreateGroupNote | ★ 创建专属任务（关联 group_id，支持选择负责人+截止日期+标签）                          |

### 5.9 协同房间

| 方法 | 路径                      | Handler     | 说明                          |
| ---- | ------------------------- | ----------- | ----------------------------- |
| GET  | `/rooms/:note_id/canvas`  | GetCanvas   | 获取画布数据                  |
| POST | `/rooms/:note_id/command` | SendCommand | 下发指令（admin/leader 权限） |

### 5.10 台账

| 方法 | 路径            | Handler | 说明                   |
| ---- | --------------- | ------- | ---------------------- |
| GET  | `/ledger`       | List    | 台账列表（多条件过滤） |
| GET  | `/ledger/stats` | Stats   | 台账统计               |

### 5.11 Bug 反馈 / Issues（★ 新增，GitHub/Gitee Issues 风格）

| 方法 | 路径                   | Handler      | 说明                                                                               |
| ---- | ---------------------- | ------------ | ---------------------------------------------------------------------------------- |
| GET  | `/issues`              | List         | 问题列表（分页 + status/type/keyword 过滤，批量统计 comment_count，倒序）          |
| POST | `/issues`              | Create       | 创建问题（title/content 必填，type 缺省 bug；自动生成自增 #编号 issue_no）         |
| GET  | `/issues/:id`          | Get          | 问题详情（issue + comments 列表，按时间正序）                                      |
| POST | `/issues/:id/comments` | AddComment   | 发言反馈（所有登录用户；问题已关闭返回 403「该问题已关闭，无法继续评论」）         |
| PUT  | `/issues/:id/status`   | UpdateStatus | 关闭/重开（仅创建人本人或 super_admin/dept_admin，其余 403；status ∈ open/closed） |

> 表结构：`issues`（id / issue_no 自增 / title / content / type∈bug|feature / status∈open|closed / user_id / user_name / 时间戳）、`issue_comments`（id / issue_id / user_id / user_name / content / created_at）；AutoMigrate 自动建表。
> 操作日志新增动作：`create_issue`（创建问题）、`add_issue_comment`（反馈评论）、`update_issue_status`（关闭/重开）。

### 5.12 工作成效分析

| 方法   | 路径                         | Handler                | 说明                                                                                                                            |
| ------ | ---------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| GET    | `/analytics/personal-stats`  | PersonalStats          | 个人统计                                                                                                                        |
| GET    | `/analytics/team-stats`      | TeamStats              | ★ 团队成效统计（date_from/date_to；user_ids 逗号分隔可选，仅在可见范围内过滤勾选成员）                                          |
| POST   | `/analytics/team-report`     | GenerateTeamReport     | ★ 团队周报/月报生成（ai_config_id 选模型：有则AI生成，无则模板生成，失败回退模板；user_ids 可选勾选成员范围，报告归属当前用户） |
| GET    | `/analytics/ai-configs`      | ListAIConfigsForReport | ★ 启用的 AI 配置简表（id/provider/model，普通用户可访问，供生成报告选模型）                                                     |
| POST   | `/analytics/daily-report`    | GenerateDailyReport    | 生成今日日报                                                                                                                    |
| POST   | `/analytics/weekly-report`   | GenerateWeeklyReport   | 生成本周周报                                                                                                                    |
| POST   | `/analytics/monthly-report`  | GenerateMonthlyReport  | 生成本月月报                                                                                                                    |
| POST   | `/analytics/ai-report`       | GenerateAIReport       | 生成智能报告（支持自定义模板）                                                                                                  |
| GET    | `/analytics/reports`         | ListReports            | 报告历史                                                                                                                        |
| GET    | `/analytics/reports/:id`     | GetReport              | 报告详情                                                                                                                        |
| DELETE | `/analytics/reports/:id`     | DeleteReport           | 删除报告                                                                                                                        |
| GET    | `/analytics/report-template` | GetReportTemplate      | ★ 获取报告模板（无则自动种子默认模板）                                                                                          |
| PUT    | `/analytics/report-template` | SaveReportTemplate     | ★ 保存报告模板（upsert）                                                                                                        |

> 团队统计口径：创建数按 creator、完成数按 owner 且已归档、平均完成耗时 = (completed_at - created_at) 均值、被盯办按 reminders.target；super_admin 见全部部门，其他角色见本部门（含子部门）。

### 5.13 系统管理（超管）

| 方法       | 路径                                 | Handler                                                                                                                                               | 说明                                                                              |
| ---------- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| GET/PUT    | `/system/config`                     | 获取/更新系统配置                                                                                                                                     |
| GET/POST   | `/system/ai-configs`                 | AI 配置列表/创建                                                                                                                                      |
| POST       | `/system/ai-configs/test`            | TestAIConfig                                                                                                                                          | ★ 一键测试连通（不落库）：dify 走 GET /info，其他走 OpenAI 兼容 /chat/completions |
| PUT/DELETE | `/system/ai-configs/:id`             | 更新/删除 AI 配置                                                                                                                                     |
| GET/PUT    | `/system/config-files` / `/:name`    | 配置文件管理                                                                                                                                          |
| GET        | `/system/config-files/:name/history` | 配置文件历史                                                                                                                                          |
| GET        | `/system/logs`                       | ListAdminLogs                                                                                                                                         | 管理日志                                                                          |
| GET        | `/system/operations`                 | ListOperations                                                                                                                                        | 操作日志                                                                          |
| GET        | `/system/operations/actions`         | GetOperationActions                                                                                                                                   | 操作类型列表                                                                      |
| GET/PUT    | `/system/chat-file-policy`           | ★ 聊天文件白名单/黑名单（单行策略表 `chat_file_policies`，保存即热加载：`services.InitFilePolicyService` 启动时加载，PUT 更新后刷新内存缓存立即生效） |

> 说明：创建/更新 AI 配置前后端强制连通性测试（`services.TestAIConnection`），失败返回 400 拒绝保存；`provider_type` 支持 openai / deepseek / qwen / zhipu / dify / custom（旧数据归一化为 openai）。

**操作日志记录范围（★ 增强）**：`OperationLogger` 中间件（`middleware/operation_logger.go`）自动记录所有 POST/PUT/DELETE 请求，`parseOperation` 按路径解析操作类型与资源 ID，覆盖：任务（create/update/delete/complete/remind/restore/feedback/upload_attachment/delete_attachment）、标签、部门、用户、模板（含 delete）、工作组（create/update/delete/成员增改删/工作组任务/生成报告/删除报告）、预设组、成效报告（团队/AI/日报/周报/月报生成、删除报告、保存模板）、通知（全部已读/标记已读/删除）、聊天（发送消息/标记会话已读）、到期提醒确认、系统配置/AI配置（含 test）/配置文件。成功操作详情为「动作描述 · 关键字段摘要」（如 `编辑任务 · title：xxx`），失败操作自动追加 HTTP 状态码；密码/API 密钥等敏感字段脱敏。

### 5.14 通知 / 聊天 / 盯办提醒（★ 新增）

**通知接口**

| 方法   | 路径                          | Handler     | 说明                                                  |
| ------ | ----------------------------- | ----------- | ----------------------------------------------------- |
| GET    | `/notifications`              | List        | 通知列表（data/total/unread_count，支持 unread_only） |
| GET    | `/notifications/unread-count` | UnreadCount | 未读数量                                              |
| POST   | `/notifications/:id/read`     | MarkRead    | 单条标记已读                                          |
| POST   | `/notifications/read-all`     | MarkAllRead | 全部标记已读                                          |
| DELETE | `/notifications/:id`          | Delete      | 删除通知（软删除）                                    |

**聊天接口（★ 支持文本 / 图片 / 文件）**

| 方法 | 路径                     | Handler              | 说明                                                                                                                                                                                  |
| ---- | ------------------------ | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GET  | `/chat/conversations`    | Conversations        | 会话列表（含未读数 + 好友姓名/头像 + 最后消息摘要与类型，按时间倒序）                                                                                                                 |
| POST | `/chat/attachments`      | UploadChatFile       | ★ 聊天文件上传（multipart `file`，≤10MB，**格式受 chat_file_policies 策略表控制**：黑名单优先拦截，白名单非空时仅允许白名单，白名单为空时除黑名单外全部放行；返回文件元数据，不落库） |
| GET  | `/chat/:userId/messages` | ListMessages         | 与指定用户的消息记录（分页 page/page_size）                                                                                                                                           |
| POST | `/chat/:userId/messages` | SendMessage          | 发送消息：`{type?: text\|image\|file, content?, file_name?, file_path?, file_size?, mime_type?, note_id?}`                                                                            |
| POST | `/chat/:userId/read`     | MarkConversationRead | 标记会话已读                                                                                                                                                                          |

**ChatMessage 表结构（聊天记录，落库持久化）**

| 字段        | 类型         | 说明                                        |
| ----------- | ------------ | ------------------------------------------- |
| id          | uuid PK      | 消息 ID                                     |
| sender_id   | uuid idx     | 发送人                                      |
| receiver_id | uuid idx     | 接收人                                      |
| note_id     | uuid 可空    | 关联任务（可选）                            |
| type        | varchar(20)  | text 文本 / image 图片 / file 文件          |
| content     | text         | 文本内容（图片/文件消息可为空）             |
| file_name   | varchar(255) | 文件名（image/file 消息）                   |
| file_path   | varchar(500) | 文件 URL（`/uploads/xxx`，image/file 消息） |
| file_size   | bigint       | 文件大小（字节）                            |
| mime_type   | varchar(100) | 文件 MIME 类型                              |
| is_read     | bool idx     | 是否已读                                    |
| created_at  | timestamptz  | 发送时间                                    |

> 发送校验：text 类型 content 必填；image/file 类型 file_path 必填，否则 400。发送成功落库后通过 WebSocket `chat:message` 实时推送给接收人。会话摘要对图片/文件消息显示 `[图片]` / `[文件] 文件名`。

**盯办提醒接口**

| 方法 | 路径                         | Handler             | 说明         |
| ---- | ---------------------------- | ------------------- | ------------ |
| GET  | `/reminders`                 | ListReminders       | 我收到的提醒 |
| POST | `/reminders/:id/acknowledge` | AcknowledgeReminder | 确认提醒     |

**WebSocket 个人通知通道**

| 方法 | 路径                | 说明                                                                  |
| ---- | ------------------- | --------------------------------------------------------------------- |
| GET  | `/ws/user/:user_id` | 个人实时通道（token 鉴权），事件：`notification:new` / `chat:message` |

> **通知触发点：** 指派任务 `task_assigned`（发给被指派者）、完成任务 `task_completed`（发给发起人）、补充反馈 `task_feedback`（发给发起人）、盯办 `task_remind`（发给被盯办人）。通知同时落库 + WebSocket 实时推送。完成/反馈权限：发起人或被指派人（assignee）。

---

## 六、架构与中间件

### 6.1 三层架构

```
┌─────────────────────────────────────────┐
│  Handler  请求解析 → 参数校验 → 调用 Service → JSON 响应   │
├─────────────────────────────────────────┤
│  Service  业务编排 / 事务管理 / 权限判断（本项目部分直接由 Handler 调用 Repo） │
├─────────────────────────────────────────┤
│  Repository  GORM 数据访问 / 查询构建 / 分页 / 事务       │
├─────────────────────────────────────────┤
│  Model  18 个 GORM 模型 / 表结构 / 关联关系定义            │
└─────────────────────────────────────────┘
```

### 6.2 中间件链

**全局中间件**：`RequestID → Recovery → SecurityHeaders → CORS → RequestLogger → RateLimit`

**认证后中间件**：`AuthMiddleware（JWT 验证 + 用户注入上下文）→ OperationLogger（自动记录操作日志）`

**权限中间件**：`RequireRoles(super_admin, dept_admin, group_leader)`

### 6.3 权限模型

- **角色**：`super_admin` / `company_leader` / `dept_admin` / `group_leader` / `member`
- **数据权限 Scope**：`global` / `department` / `group` / `self`
- 任务查询时自动按用户角色附加 WHERE 条件（owner_id / creator_id / department_id / group_id）
- `note.Service` 的 `buildScopeQuery` 函数根据角色动态构建查询条件
- **★ 公司领导（company_leader）**：权限矩阵位于 `auth_service.getPermissions`——全局数据范围（`view_all_archive`、`checkAccess` 短路放行全公司任务详情），拥有任务全操作、部门/用户/标签/模板**增改**、数据大屏与指令、专属 `inspect_user_workbench`；**不含 `manage_system`**（`/system/*` 配置与操作日志接口仍为 super_admin 独占，company_leader 返回 403）；**无删除权限**（`DELETE /users|departments|presets` 仍为 super_admin 独占）

---

## 七、核心业务逻辑

### 7.1 一键创建专项工作组（Create WorkGroup）

1. 接收请求：`name / description / template_type / due_time / members[{user_id,role,sub_group_name}] / tags[]`
2. 生成工作组 UUID，插入 `work_groups` 表
3. 遍历 members：
   - 插入 `work_group_members` 记录
   - 生成 sequence 获取年度序列号
   - 创建 Note（`source_type=assigned`、`group_id` 关联工作组、`owner_id` 为成员、`due_time` 继承、`tags` 继承）
   - 创建 `NoteAssignee` 关联（`role_in_note` 标明组长/组员）
4. 返回完整工作组数据（含成员）

### 7.2 工作组专属任务（Group Notes）

- `GET /groups/:id/notes`：调用 `NoteRepository.ListByGroup(groupID, userID, page, pageSize)`
  - 查询 `notes WHERE group_id = ? AND (owner_id = ? OR creator_id = ?)`
  - Preload Tags / Assignees / Group / Attachments / Reminders
- `POST /groups/:id/notes`：创建任务时强制绑定 `group_id`，支持 `tag_ids` 标签关联

### 7.3 工作组搜索（Search Groups）

- 查询 `GET /groups?keyword=&user_id=&date_from=&date_to=&page=&page_size=`
- `keyword` 对 name 和 description 执行 ILIKE 匹配
- `user_id` 筛选"该用户发起 + 该用户作为成员"的工作组
- `date_from / date_to` 时间范围过滤
- Preload Members.User 和 Initiator

### 7.4 序列号生成

- `NoteRepository.GetNextSerialNumber(year)`：原子性 `INSERT...ON CONFLICT UPDATE` 自增
- 格式：`ZP{YYYY}-{序号}`（如 `ZP2026-0001`）

### 7.5 默认报告模板种子

- `main.go` 启动时执行 `FirstOrCreate`，若数据库无默认模板则自动写入
- 首次 `GET /analytics/report-template` 也会自动创建默认模板

---

## 八、安全规范

- **密码**：bcrypt 加密存储
- **JWT**：RS256 签名，Access Token 2h，Refresh Token 7d
- **接口限流**：基于令牌桶的 RateLimit 中间件
- **SQL 注入**：GORM 参数化查询，禁止字符串拼接
- **操作审计**：OperationLogger 中间件自动记录所有 POST/PUT/DELETE 操作

---

_文档结束_
