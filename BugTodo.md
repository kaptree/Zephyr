Bug1: 目前标签库的管理存在问题，新增一级、二级标签创建完成后都会消失，标签选中后无法删除，请添加删除功能，删除标签后将所有带有该便签的任务的标签也进行删除，保持标签一致性。

- ✅ 已完成（2026-08-17）
- 根因：
  - 后端 `TagRepository.FindAll` 存在错误逻辑：使用次数为 0 的标签被自动 DELETE，且列表只返回「被任务使用」的标签 → 新建的一级/二级标签刷新后即消失
  - 前端标签库页 `addTag` 仅在本地数组 unshift（假添加），未调用后端接口
  - 后端删除接口 `IsInUse` 拒绝删除「使用中」的标签 → 已使用的标签无法删除
- 修复内容：
  - `FindAll`：移除自动删除与「仅返回已使用」逻辑，返回全部标签（含未使用），使用次数批量统计
  - `Delete`：改为事务级联删除——清理 `note_tags` 关联（含子标签）→ 删除子标签与自身，任务上不再残留该标签，保持一致性；删除接口移除「使用中禁止删除」限制
  - 前端标签库页：`addTag` 改为调用 `createTag` API 真实入库并刷新列表；标签卡片新增删除按钮（confirm 确认后调 `deleteTag`）
- 验证：接口测试（新建一级/二级标签刷新保留、删除使用中标签返回 200、任务标签关联级联清理）+ 浏览器验证（新建刷新保留、删除后列表移除、任务上标签同步清理、暗色可读、无报错）

Bug2：当一个系统管理员向一个部门管理员发送指派任务的时候，部门管理员的账号能收到任务但是工作台上不能显示出来，需要修复。并检查是否存在同类的部门管理员互相发送指派任务无法显示的情况，以及系统管理员互相发送指派任务无法显示的情况。

- ✅ 已完成（2026-08-18）
- 根因：
  - 任务创建时 `notes.department_id` = 创建者所在部门；被指派者写入 `note_assignees`（通知按被指派者发送，因此「能收到任务」）
  - 但 `NoteRepository.List` 的 `dept_admin` 分支此前**仅按「任务部门 ∈ 本部门（含子部门）」过滤**，未包含「被指派人 / 创建人 / 负责人」维度 → 跨部门指派的任务（如系统管理员派给部门管理员）在部门管理员工作台列表被过滤掉，与 `CheckUserAccess`（被指派人始终可访问）语义不一致
  - 同类问题：部门管理员互相跨部门指派同样不显示；super_admin 走 default 分支（已含被指派人），系统管理员互相指派本可显示（逻辑正确，验证确认）
- 修复内容：
  - `NoteRepository.List` 的 `dept_admin` 分支改为：`任务部门 IN 本部门（含子部门） OR 创建人 = 我 OR 负责人 = 我 OR 被指派人 = 我`，与详情访问权限 `CheckUserAccess` 语义一致
- 验证（接口实测）：
  - super_admin → dept_admin 指派：dept_admin 工作台显示 ✅ + 收到通知 ✅
  - dept_admin → 另一 dept_admin 指派：被指派的 dept_admin 显示 ✅
  - dept_admin → super_admin 指派（等效 super↔super 的 default 分支）：super_admin 作为被指派人显示 ✅
  - 回归：普通用户列表仅本人相关、dept_admin 不出现其他部门无关任务 ✅

Bug3:现在的聊天页面，无论对方是否上线都显示在线状态，需要进行修复，并且发出的消息可以看到对方是否已读和未读

- ✅ 已完成（2026-08-18）
- 根因：
  - 在线状态：Hub 的 `broadcastPresence` 仅在「同一用户自己的多连接房间」内广播（协作房用途），无法感知其他用户是否在线；聊天页顶部硬编码「在线」
  - 已读回执：消息表有 `is_read/read_at` 字段且已支持标记已读，但标记后不实时通知发送方，前端气泡也未展示已读/未读
- 修复内容：
  - 后端 `ws/hub.go`：新增 `IsUserOnline / OnlineUserIDs / BroadcastPresence`（用户上线/下线时向所有在线用户广播 `presence:update` 在线 ID 列表）
  - 新增接口 `GET /api/v1/chat/online` 返回当前在线用户 ID 列表（前端进入聊天页/打开会话时拉取）
  - 已读回执：`MarkConversationRead` 标记已读后通过 WS 推送 `chat:read` 事件给发送方
  - 前端 `stores/notification.ts`：维护 `onlineIds`，处理 `presence:update`（在线状态实时刷新）与 `chat:read`（自己发出消息标记已读）
  - 聊天页/抽屉：顶部真实显示「在线/离线」（绿点/灰点）、会话列表头像在线绿点、自己消息下方显示「已读/未读」
  - 登出/切换账号时关闭 WebSocket（`auth.logout/login` 调 `disconnect()`），登出用户立即从在线列表移除
- 验证（浏览器双标签实测）：zhang 未登录 admin 端显示「离线」→ zhang 登录后实时变「在线」+ 会话绿点；admin 发消息显示「未读」→ zhang 打开会话后实时变「已读」；第二条消息同样流转；暗色模式配色正常；无报错

bug4:在创建任务进行选择指派人员和抄送人员的时候，不能重复选择同一个用户，选了指派再选择抄送则会提示，并且不会勾选，还有就是，反之也不行，还有就是点击选择人的列表弹出来后，点击空白处也能关闭列表。请修复这两个bug

- ✅ 已完成（2026-08-18）
- 根因：
  - 指派与抄送是两个独立的 `UserPicker`，互不知道对方已选的人，同一用户可在两边各选一次（创建后 backend 数据中同人既是指派又是抄送）
  - `UserPicker` 下拉列表没有点击外部关闭的处理，只能点「完成」或再次点触发按钮关闭
- 修复内容：
  - `UserPicker.vue`：新增 `disabledIds`（不可选用户列表）与 `disabledNote`（提示文案）属性——禁用用户行显示灰色半透明 + 标注文案（如「已在指派人员中」），点击时弹出警告 Toast「该用户…不能重复选择」且不会勾选；部门「全选」自动排除禁用用户；新增 `onDocClick` 点击组件外部空白处自动关闭下拉列表（挂载/卸载时注册/移除 document 监听）
  - `WorkbenchPage.vue`：指派选择器传入 `:disabled-ids="selectedCcIds"`、`disabled-note="已在抄送人员中"`；抄送选择器传入 `:disabled-ids="selectedAssigneeIds"`、`disabled-note="已在指派人员中"`，双向互斥
- 验证（浏览器实测）：先指派张三再在抄送中搜张三 → 灰色不可选 + 点击弹出「该用户已在指派人员中，不能重复选择」且不勾选；反之先抄送刘探员再在指派中搜刘探员 → 同样拦截；点击列表外部空白处关闭、点击列表内部保持打开、点「完成」关闭；创建任务成功；控制台无报错
