1、表格模块化美化。为Zephyr工作台的表格组件设计一套“呼吸式数据流”交互方案。表格行不再只是静态数据展示，而是具备以下动态特征：
行入场动画：新数据行插入时，从左侧以 300ms 的“滑动 + 淡入”效果进入，背景色从主题色（primary/20）渐变为透明，形成“新鲜数据”的视觉提示。
状态突变反馈：当任务状态（待办→完成→盯办）发生变化时，对应行以“脉冲光晕”动画闪烁 600ms，颜色随状态变化（黄色→绿色→红色）。
悬停微交互：鼠标悬停时，行背景以 200ms 平滑过渡为浅色主题色，并伴有轻微的“上浮”位移（translateY(-2px)）和阴影加深，营造卡片式悬浮感。
表格头部固定与滚动：长表格滚动时，表头优雅吸顶，滚动条采用自定义细条样式，滚动时内容行以“视差”效果轻微错位，增强浏览沉浸感。
空状态动画：无数据时，展示带有呼吸动画的插画占位，占位元素以 2s 周期缓慢脉动，配合“暂无数据，点击创建第一条任务”的引导文案。
技术实现建议：使用 Vue 3 的 <TransitionGroup> 包裹 <tbody> 中的行，配合 Tailwind CSS 自定义 @keyframes 动画类实现。

2.为 Zephyr 构建一套“微交互动画语言系统”，让每一次操作都有细腻的反馈，针对网站内容：
按钮点击涟漪：所有主要按钮点击时，从点击位置向外扩散圆形涟漪（ripple effect），持续 400ms，颜色为按钮主色的半透明版本，营造“触达”感。✅ 已完成：全局事件委托（utils/ripple.ts），覆盖 btn-primary/btn-secondary/bg-blue-500/600/品牌蓝 #3B82F6 按钮，400ms ripple-ink 扩散，支持 prefers-disabled 跳过
加载骨架屏：数据加载时，使用带有“流动渐变”效果的骨架屏（skeleton），渐变条以 1.5s 周期从左向右滑动，模拟“数据正在填充”的视觉效果。✅ 已完成：.skeleton 渐变 shimmer（1.5s skeleton-shimmer，明暗双主题），应用于 Analytics 统计卡（5+4 卡）、操作日志表格行、Issues 列表、数据大屏 4 卡
数字滚动动画：统计卡片（任务数量、完成率等）中的数字变化时，以“计数器”方式逐位滚动更新，时长 600ms，配合 ease-out 缓动函数。✅ 已完成：AnimatedNumber 通用组件（requestAnimationFrame + cubic ease-out），接入 Analytics 个人 5 卡/团队 4 卡、数据大屏 4 卡、个人中心 4 卡，支持 format 格式化（百分比/小时/千分位）
状态标签切换：任务状态标签（待办/完成/盯办）切换时，标签以 300ms 的“翻转”动画过渡，正面显示旧状态，翻转后显示新状态。✅ 已完成：badge-flip 300ms 翻转（tailwind animation），StickyNoteCard 5 类徽章 + Workbench 详情弹窗 4 徽章，:key 变化触发重放
通知小红点：未读消息/任务的小红点以“呼吸脉冲”动画呈现，每 2s 脉动一次，吸引注意但不打扰。✅ 已完成：animate-breathe 应用于 NotificationBell 未读红点、TopBar 聊天红点及 4 处空状态占位
主题切换动画：明暗主题切换时，使用 View Transition API 实现圆形扩散/收缩的过渡效果，从点击位置向外扩散。✅ 已完成：useDarkMode 集成 startViewTransition + clipPath circle() 从点击位置扩散 500ms，prefers-reduced-motion 自动降级

3.为 Zephyr 的路由页面切换设计一套“空间感知”动画系统：

层级导航动画：从列表页进入详情页时，详情页以“从右向左滑动 + 放大”的方式进入（400ms），同时列表页以“缩小 + 左移”的方式退出，营造“下钻”的深度感。✅ 已完成：ROUTE_DEPTH 路由层级表（router/index.ts afterEach 推导方向），slide-forward 过渡（进入 0.4s cubic-bezier(0.16,1,0.3,1) translateX(48px)+scale(0.98)，退出缩小左移），接入 AdminLayout 全局路由过渡，浏览器实测归档列表→详情捕获生效

返回动画：从详情页返回列表页时，执行反向动画——详情页“从左向右滑出并缩小”，列表页“从右向左滑入并放大”，形成完整的空间导航闭环。✅ 已完成：slide-back 反向过渡（进入 0.3s / 退出 0.2s ease-in），深度对比自动判向，实测详情→列表捕获 slide-back-leave/enter-active 生效

Tab 切换：底部/侧边 Tab 切换时，内容区域以“淡入淡出 + 轻微上移”（300ms）过渡，顶部标签指示器以平滑滑动（slide）方式移动。✅ 已完成：tab-swap 全局过渡（0.3s fade + translateY 上移）接入工作台/成效分析 Tab 内容区；Analytics 滑动指示条（absolute + transition-[left,width] 300ms，offsetLeft/offsetWidth 测量 + watch/resize 跟随），实测切换捕获 tab-swap 动画类且指示条 left 0px→114px 平滑滑动

模态页面切换：全屏模态页面（如任务编辑）从底部以“上滑 + 淡入”方式出现（350ms，ease-out），关闭时“下滑 + 淡出”（300ms，ease-in），模拟移动端底页效果。✅ 已完成：modal-sheet 全局过渡（遮罩 0.3s 淡入淡出 + .modal-panel 面板 enter 0.35s ease-out 上滑 / leave 0.3s ease-in 下滑，:duration 显式声明），接入工作台 3 个模态（新建任务/详情编辑/专项工作组）+ Bug 反馈新建模态，实测捕获 enter/leave 动画类且面板 transform 过渡生效

KeepAlive 缓存：对频繁访问的页面（工作台、任务列表）使用 <KeepAlive> 缓存，切换时无加载白屏，瞬间呈现。✅ 已完成：AdminLayout <keep-alive :include="['WorkbenchPage','IssuesPage']"> + 组件 defineOptions 命名匹配，DOM 标记实测来回切换（工作台↔Bug 反馈↔人员管理）标记全程保留、组件零重新挂载

4.为 Zephyr 的所有“关闭/退出”操作设计统一的“优雅退场”动画体系：

弹窗/抽屉关闭：关闭时，元素以“收缩 + 淡出”的方式退场（250ms），收缩方向与打开方向相反（右滑打开的向左收缩，上滑打开的向下收缩）。✅ 已完成：shrink-out 全局 Vue 过渡（leave 0.25s ease-in scale(0.92)+淡出），接入 23 处弹窗/浮层（admin 5 页 8 弹窗、GroupDetail 4、Analytics 1、NotificationPage 2、InspectWorkbench 1、ChatPage 2、NotificationBell 2、FeedbackModal 1、TagSelector 1、UserPicker 1），实测任务反馈弹窗/新建任务弹窗关闭捕获 leave-active 生效

页面关闭：浏览器标签页/窗口关闭前，触发一个 500ms 的“收拢”动画——页面内容从四周向中心收缩并淡出，模拟“文件归档”的隐喻。✅ 已完成：App.vue beforeunload + visibilitychange 双触发 #app 加 page-fold-out（500ms scale(0.82)+淡出 forwards），实测 #app 类注入动画生效（anim page-fold-out 0.5s，end opacity 0）

任务归档：任务被归档时，卡片以“折叠 + 淡出”动画消失（400ms），折叠方向从底部向上，配合轻微的纸张翻动音效（视觉代替）。

删除操作：删除数据时，被删除项以“红色闪烁 → 缩小 → 淡出”三步动画（600ms）退场，让用户明确感知删除已完成。

会话关闭：聊天窗口关闭时，以“从右下角收缩为一个小点”的动画消失（300ms），模拟“收起对话”的物理感。

5.为 Zephyr 的弹窗和消息提醒设计一套“轻量级通知美学”：

右上角消息弹窗 ✅：新任务/新消息从右上角以“滑入 + 淡入”方式出现（400ms，ease-out），停留 4 秒后以“滑出 + 淡出”方式消失（300ms，ease-in）。弹窗带有微弱的毛玻璃背景（backdrop-blur）和阴影，不遮挡主内容。

模态对话框 ✅：居中模态框出现时，背景遮罩以 300ms 淡入，对话框本体以“从中心放大 + 淡入”（scale 0.9 → 1.0，opacity 0 → 1）的方式呈现，时长 350ms，ease-out。

Toast 通知 ✅：底部/顶部 Toast 通知以“从边缘滑入”的方式出现（300ms），带有进度条倒计时指示器，进度条以线性方式在 3s 内走完，走完后通知自动消失。

工具提示（Tooltip）✅：悬停时工具提示以“上浮 + 淡入”（200ms）出现，位置跟随鼠标或元素，带有小三角指示箭头，字体为系统默认的 12px。

确认对话框 ✅：危险操作（删除、归档等）的确认对话框，按钮带有“警告红”色，取消与确认按钮以 200ms 的“错位出现”动画依次呈现，引导用户认真确认。

离线检测双横幅 ✅：顶部横幅以“从顶部滑入”的方式出现（400ms），并带有白色圆点脉冲动画。两种状态区分：网络级断开（navigator.onLine）→ 琥珀色“离线模式”横幅；后端不可达（每 15s 轮询 /api/v1/ping 校验连通性，5s 超时）→ 橙色“离线缓存提醒”横幅。网络断开后端必然不可达，联动置为离线缓存提醒；网络恢复时立即重新探测后端。
