# Zephyr 平台（轻燕工作台）全功能测试报告

| 项目 | 内容 |
| --- | --- |
| 测试对象 | Zephyr 情指行一体化平台（Server-code 后端全量 API） |
| 测试日期 | 2026-09-02 |
| 测试方式 | 黑盒 API 自动化测试（bash + curl + jq），覆盖正向流程、越权访问、非法输入负例 |
| 测试环境 | macOS / Go 1.22+ / Gin + GORM / PostgreSQL(labelpro) / Redis / 服务端 `127.0.0.1:8090` |
| 测试账号 | admin(super_admin)、wang(dept_admin)、li(group_leader)、zhang3(member)，密码均为 `Admin@123` |
| 测试脚本 | `Server-code/.gotmp/api_test.sh`，请求调试日志 `Server-code/.gotmp/api_debug.log` |
| **最终结论** | **147 项用例全部通过（147 PASS / 0 FAIL），无遗留失败项** |

---

## 一、测试范围

覆盖平台全部 18 个功能域，共 147 个测试点：

| 模块 | 用例数 | 覆盖内容 |
| --- | --- | --- |
| 基础与认证 | 13 | health/ping、未授权 401、无效 token 401、错误密码 401、四角色登录、token 刷新、当前用户信息 |
| 部门管理 | 6 | 部门树/详情/增/改/删、member 越权 403 |
| 用户管理 | 16 | 列表/可见用户/统计/人员推荐/详情/档案、创建/重复名 400/弱密码 400/member 越权 403、更新/越权更新 403、自助资料、删除 |
| 标签管理 | 4 | 增删改查 |
| 模板管理 | 5 | 列表、创建人增/改/删、member 删除他人模板被拒 403 |
| 任务（便签） | 19 | 列表/统计/热力图、创建/详情/更新/反馈、签收权限（self 拒 403、assigned 被指派人可签）、盯办权限（非参与人拒 403/404、发起者盯被指派人成功）、附件上传(png)/非法类型(txt 400)/列表/删除、完成、归档删除、**恢复**、导出 Excel |
| 工作组 | 14 | 搜索/我的组/详情/成员/任务/看板/报告、创建/加成员/改角色/移除成员/组内任务/组报告/删除 |
| 协同房间（画布） | 6 | 画布类任务自动建房并获取画布、指令历史、发送指令、指令持久化校验、member 越权 403 |
| 预设组 | 5 | 列表/增/改/删、member 越权 403 |
| 台账 | 2 | 列表、统计 |
| 通知 | 5 | 列表/未读数/标记已读/删除/全部已读 |
| 即时聊天 | 6 | 会话列表、在线状态、发消息、聊天记录、已读标记、聊天附件上传 |
| 表情包 | 3 | 列表、上传自定义表情(png)、删除 |
| 提醒 | 1 | 列表 |
| Issue | 10 | 列表、创建、详情、评论、关闭、订阅/退订、全局订阅/查看/取消 |
| 数据分析 | 12 | 个人/团队统计、团队报告、AI 配置列表、报告列表/详情/子页、报告模板读写、日报/周报/月报 |
| 系统管理 | 16 | member 越权 403、系统配置读写、AI 配置增/改/连通测试/删（mock 端点）、配置文件列表/读取/历史、管理日志/操作日志/操作类型、聊天文件策略读写 |
| 其他 | 4 | WebSocket 端点可达、静态资源、404 路由 JSON 处理、配置文件密码未被掩码覆盖 |

---

## 二、发现的缺陷与修复（5 个，均已修复并回归验证）

### 缺陷 1：已归档任务无法恢复（权限校验层）

- **现象**：任务归档（软删除）后调用 `POST /notes/:id/restore`，即使原发起人操作也返回 404。
- **根因**：恢复接口的访问校验 `CheckUserAccess` 内部查询被 GORM 软删除作用域过滤，对已软删行查不到参与关系，误判为无权限。
- **修复**：
  - `internal/repository/note_repo.go` 新增 `CheckUserAccessIncludeDeleted`（`Unscoped` 查询）；
  - `internal/services/note_service.go` 新增 `CanAccessIncludeDeleted`；
  - `internal/handlers/note_handler.go` 的 `RestoreNote` 改用含已删除的访问校验。

### 缺陷 2：恢复接口 500（GORM 软删除 UPDATE 作用域）

- **现象**：权限层修好后恢复接口仍失败，返回 500，日志 `record not found`。
- **根因**：GORM 软删除模型执行 UPDATE 时自动附加 `deleted_at IS NULL`，导致对已软删行的恢复 UPDATE 永远匹配 0 行。
- **修复**：`internal/repository/note_repo.go` 的 `Restore` 改为 `Unscoped()` 执行更新：

```go
func (r *NoteRepository) Restore(id string) error {
	uid, err := uuid.Parse(id)
	if err != nil {
		return err
	}
	// GORM软删除模型会为UPDATE自动附加 deleted_at IS NULL 条件，
	// 导致对已软删行的恢复UPDATE永远匹配0行，必须Unscoped绕过
	return r.db.Unscoped().Model(&models.Note{}).Where("id = ?", uid).
		Updates(map[string]interface{}{
			"is_archived":  false,
			"archive_time": nil,
			"deleted_at":   nil,
		}).Error
}
```

### 缺陷 3：画布类任务 API 创建后协同房间缺失

- **现象**：通过 API 直接创建 `emergency_canvas` 等画布类任务后，进入协同房间无画布数据（房间未创建）；仅前端页面创建路径正常。
- **根因**：服务端 `Create` 未对画布类模板任务做协同房间初始化，该逻辑此前只存在于前端调用链。
- **修复**：`internal/services/note_service.go` 新增 `collabTemplateTypes`（emergency_canvas / collaborative_writing / data_analysis），创建任务时按模板类型/canvas_config 自动创建协同房间。
- **回归验证**："创建画布类任务 → 自动建房并获取画布 → 指令历史 → 发送指令 → 指令持久化" 全链路 6 项用例通过。

### 缺陷 4：限流器"死锁"——正常使用后登录被永久拒绝（429）

- **现象**：用户登录时提示"操作过于频繁，请稍后再试"，但实际并未频繁登录，且长时间无法恢复。
- **根因**（`internal/middleware/ratelimit.go` 原实现存在三个问题）：
  1. **限流死锁**：以"最近请求时间"判定 1 分钟窗口过期，但每次请求（包括被 429 拒绝的请求）都会刷新该时间戳，导致持续访问时计数**永不重置**；累计请求数超过 `api_per_minute`（1000）后陷入永久 429，只有完全静默 1 分钟才能自愈。
  2. **登录与 API 共享计数器**：`login_per_minute`（200）与 `api_per_minute`（1000）使用同一 IP 计数，正常使用平台超过 200 个请求后登录必被拒。
  3. **`ban_duration_seconds` 配置未被使用**，且访客 map 无清理存在内存泄漏。
- **修复**：重写为固定窗口限流——以"窗口起始时间"到期必重置（与请求行为解耦）；登录/普通 API 独立计数；触发限流后按 `ban_duration_seconds`（60s）封禁、期满自动恢复；增加惰性清理防内存泄漏。
- **专项验证**（`.gotmp/ratelimit_test.sh`）：

| 验证点 | 期望 | 结果 |
| --- | --- | --- |
| 正常登录 | 200 | ✅ 200 |
| 1050 个请求打满窗口 | 前 1000 个通过、其余 429 | ✅ 1000×200 / 50×429 |
| 封禁期内再请求 | 429 | ✅ 429 |
| 等待 65 秒（封禁+窗口过期）后 | 自动恢复 200 | ✅ 200（旧实现为永久 429） |
| 250 次 API 后登录（独立计数） | 登录 200 | ✅ 200（旧实现共享计数 251>200 会 429） |

### 功能增强：限流参数纳入系统设置，管理员可在线调整

- **需求**：限流参数写入系统设置，管理员可随时调整。
- **现状问题**：前端"系统管理 → 系统设置"页虽已能展示/编辑 `rate_limit` 分组（`PUT /api/v1/system/config` 热加载并持久化到 config.json），但限流中间件在启动时捕获了配置指针，`ReloadConfig()` 替换新指针后中间件仍读旧值——**管理员改了也不生效**，必须重启服务。
- **修复/实现**：
  1. `internal/middleware/ratelimit.go`：每次请求动态读取 `config.GetActive().RateLimit`，`enabled`、`api_per_minute`、`login_per_minute`、`ban_duration_seconds` 四项参数修改后**无需重启立即生效**；阈值 ≤0 视为不限制（防止误配置把所有用户锁死）。
  2. `internal/router/router.go`：适配 `middleware.RateLimit()` 新签名。
  3. `Web-Front/src/pages/admin/SystemSettingsPage.vue`：系统设置页限流字段显示中文标签（启用接口限流 / 普通API每分钟请求上限 / 登录接口每分钟请求上限 / 触发限流后封禁时长（秒））。
- **热更新专项验证**（`.gotmp/ratelimit_hotreload_test.sh`，全程不重启服务）：

| 验证点 | 期望 | 结果 |
| --- | --- | --- |
| 改 `api_per_minute=5` 后连发请求 | 低阈值立即生效（出现 429） | ✅ 2×200 / 4×429 |
| 改 `ban_duration_seconds=3` | 封禁快速到期，窗口未重置时仍限流 | ✅ 429 |
| 1 分钟窗口过期后 | 自动恢复 200 | ✅ 200 |
| 改 `login_per_minute=2` 后连登 3 次 | 2×200 + 1×429 | ✅ 精确命中 |
| 改 `enabled=false` 后连发 8 次 | 全部放行 | ✅ 0×429 |
| 恢复默认配置后 ping/登录 | 均 200 | ✅ |

### 缺陷 5：工作台"新增任务"浮动按钮未固定在右下角（前端）

- **现象**：工作台页面的"新增任务"浮动按钮（FAB）不固定在视口右下角，随页面内容滚动。
- **根因**：`style.css` 中涟漪微交互规则 `button[class*='bg-[#3B82F6]'] { position: relative; ... }` 的选择器特异性为 (0,1,1)，高于 Tailwind 工具类 `.fixed` 的 (0,1,0)。FAB 的 class 恰好含 `bg-[#3B82F6]`，其 `position: fixed` 被覆盖为 `relative`，从而跟随文档流滚动。
- **修复**：
  1. `Web-Front/src/style.css`：涟漪定位规则改为 `button[class*='bg-[#3B82F6]']:not(.fixed)`，排除 fixed 悬浮按钮；
  2. `Web-Front/src/utils/ripple.ts`：涟漪事件委托选择器同步排除 `.fixed`（涟漪 ink 依赖宿主 relative 定位与 overflow 裁剪，fixed 按钮不参与，避免 ink 溢出按钮外）。
- **浏览器实测验证**（Vite dev server，http://localhost:3000/workbench）：

| 验证点 | 期望 | 结果 |
| --- | --- | --- |
| FAB computed `position` | `fixed`（修复前为 `relative`） | ✅ fixed |
| 初始坐标 | 视口右下角（right-8/bottom-8 = 32px 内边距） | ✅ (584, 564)，viewport 672×652 精确贴合 |
| 内容区滚动 400px 后 FAB 坐标 | 不变（固定于视口） | ✅ (584, 564) 不变 |
| 截图视觉确认 | FAB 位于内容区右下角 | ✅ |
| 点击 FAB | 弹出"新建任务"模态框 | ✅ |
| 普通品牌蓝按钮涟漪定位 | 仍为 relative（涟漪不受影响） | ✅ |

### 涉及修改文件

| 文件 | 变更 |
| --- | --- |
| `Server-code/internal/repository/note_repo.go` | 新增 `CheckUserAccessIncludeDeleted`；`Restore` 使用 `Unscoped()` |
| `Server-code/internal/services/note_service.go` | 新增 `CanAccessIncludeDeleted`、`collabTemplateTypes` 及建房逻辑 |
| `Server-code/internal/handlers/note_handler.go` | `RestoreNote` 改用含已删除的访问校验 |
| `Server-code/internal/middleware/ratelimit.go` | 重写限流器：固定窗口、登录/API 独立计数、封禁期自动恢复、惰性清理；动态读取配置支持热更新 |
| `Server-code/internal/router/router.go` | 适配 `RateLimit()` 新签名 |
| `Web-Front/src/pages/admin/SystemSettingsPage.vue` | 系统设置页限流字段中文标签 |
| `Web-Front/src/style.css` | 涟漪定位规则排除 `.fixed` 按钮（修复 FAB 被 `position: relative` 覆盖） |
| `Web-Front/src/utils/ripple.ts` | 涟漪事件委托选择器排除 `.fixed` 按钮 |

---

## 三、测试过程问题（测试脚本侧，非平台缺陷）

| 问题 | 处理 |
| --- | --- |
| bash 特殊只读变量 `GROUPS` 导致"工作组搜索"断言取值恒为空（手动复现却一直通过，具有迷惑性） | 变量改名 `GRP_LIST` |
| Issue 创建请求字段用 `description`，服务端定义为 `content` | 按接口定义修正 |
| AI 配置创建字段名错误（`name/provider/model` → `provider_name/api_endpoint/model_name`，`api_endpoint` 为必填） | 按接口定义修正并指向 mock AI 端点 |
| 表情上传误用 txt 文件（服务端校验仅允许 png/jpg/gif/webp，返回 400 属正确行为） | 改用 1x1 合法 PNG |
| leader 盯办 member 自建任务返回 404——属权限设计（非任务参与人无访问权），原断言期望 200 有误 | 断言改为 403/404，并补充 assigned 流程中"发起者盯办被指派人"正向用例 |
| 多值断言（200\|201）失败时显示"实际空"，掩盖真实状态码 | 断言函数增加回退显示原始 code |
| 历史测试用户残留导致重复键冲突 | 测试用户名加时间戳唯一化 |

---

## 四、安全与权限验证要点

- 认证：无 token / 无效 token 一律 401；错误密码 401。
- 角色越权：member 创建部门/用户/预设组/访问系统配置均被拒 403；member 删除他人模板被拒 403。
- 数据权限：非任务参与人对任务详情/盯办无访问权（404）；模板删除限创建人；self 任务发起者不可签收，仅 assigned 任务被指派人可签收。
- 输入校验：弱密码 400、重复用户名 400、非法附件类型 400（含魔数校验）、AI 配置连通性预检失败 400。
- 敏感信息：AI Key AES 加密存储、配置文件密码不被掩码逻辑破坏、响应不回传密码哈希。

---

## 五、最终回归结果

- 执行轮次：首轮 115/15 → 修复后多轮回归 → **最终 147/147 全部通过**。
- 4 个服务端缺陷修复均已编译进新二进制并经回归用例验证（任务恢复、画布建房、指令持久化、限流自动恢复均 PASS）。
- 服务端运行状态：`Server-code/.gotmp/zephyr-server`（127.0.0.1:8090），health 检查正常。

## 六、遗留建议（非阻塞）

1. 协同房间的 WebSocket 实时推送建议补充并发压测与断线重连专项测试（本轮仅验证端点可达与 REST 侧持久化）。
2. AI 连通性测试在创建/更新时同步执行真实外呼，弱网环境可能拖慢请求，建议后续增加超时配置项。
3. 建议为任务恢复、画布建房等关键路径补充单元测试，防止 GORM 软删除作用域类问题回归。
